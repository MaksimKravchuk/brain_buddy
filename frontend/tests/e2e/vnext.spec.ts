import { test } from "../allure.fixtures";

test.describe("vNext operation proposal acceptance", () => {
  test.fixme(
    "E2E-VNEXT-01 confirmation gate keeps model-backed proposals provisional until explicit approval",
    async () => {
      // Product operation UI is intentionally not implemented yet. Keep this
      // charter scenario visible so the future implementation has a reserved
      // browser acceptance slot without making today's CI non-deterministic.
    }
  );
});
