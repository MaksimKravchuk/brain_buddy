# The ENFORCED frontend mutation tier (ADR-0013): modules whose score may block
# a pull request. Narrower than `mutate` in stryker.config.json, which is the
# OBSERVED tier the nightly campaign measures.
#
# A module joins this list only after clearing ADR-0004's bar on its own -- at
# least 95% across two consecutive successful scheduled runs against an
# unchanged allow-list. Adding one without that evidence defeats the point of
# having two tiers.
#
# NOT YET ENFORCING. Nothing reads this list in CI yet, and it must stay that
# way until two consecutive nightlies have recorded the score. The measurement
# below is a single local run, which satisfies only the score half of the
# precondition.
#
# Paths are relative to frontend/, matching the keys Stryker writes into
# mutation-artifacts/mutation-report.json.
#
# Measured locally over exactly this list on 2026-08-10:
#   542 of 551 mutants killed -- 98.37%.
#
# Observed but deliberately NOT enforced yet, with the same measurement:
#   src/features/tasks/smartAdd.ts                  96.23% (536/557)
#   src/api/taskHooks.ts                            93.51% (72/77)
#   src/api/account.ts                              93.33% (28/30)
#   src/features/brain-dump/brainDumpNavigation.ts  91.67% (11/12)
#
# smartAdd.ts now clears ADR-0004's 95% on its own and is the first candidate
# for promotion -- but it is NOT promoted here. Clearing the score satisfies one
# half of the precondition; the other half is two consecutive successful
# scheduled runs, and the nightly had not yet produced one when this was
# measured. Promote it once two nightlies have recorded >= 95% against this
# unchanged list, and record their run URLs here when you do.
src/api/auth.ts
src/api/client.ts
src/stores/authStore.ts
src/utils/error.ts
src/utils/telemetry.ts
