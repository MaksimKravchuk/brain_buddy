# Delivery report: 014-a2a-relay-wire-contract

**Branch**: `feat/014-a2a-relay-wire-contract`  **HEAD**: `a263719`

> Generated by `scripts/render_feature_report.py`. Every field below is read from an artifact on disk. A stage that did not run is marked _not run_ rather than omitted.

---

### 1-2. The ask and what was agreed

`intake.md` is present.

**The ask, as given**

> надо сделать a2a, ты оркестратор. Доведи дело до конца, используй субагентов

Preceded in the same session by: "Ок, прогони assess-research по A2A", which produced
`.specify/assessments/a2a-relay-wire-contract/research.md`. Stage 0 note: the define,
shape and decide stages ran in parallel with this interview; the verdict in
`decision.md` is checked before `/speckit-specify` runs, and this intake is not adopted
into a feature directory unless that verdict is `go`.

**4. Scope boundary**

**In scope**

- [ ] Replace the bespoke connector envelope with the A2A v1.0 protocol as the only wire contract; the generic HTTP connector is removed (it has zero consumers).
- [ ] Connect an A2A agent by its Agent Card, with bearer or API-key authentication, over public HTTPS.
- [ ] Disclose what the agent supports (streaming, push notifications, skills) and whether it implements the optional BrainBuddy extension.
- [ ] Hand off one task without blocking the user: BrainBuddy keeps the agent call alive in the background and treats an authenticated poll as the source of truth; push notifications only wake the poll.
- [ ] Guard against duplicate starts on BrainBuddy's side (look the run up by its context before any resend) and disclose the residual risk honestly for agents without the extension; agents with the extension get the strong guarantee.
- [ ] Optional BrainBuddy A2A extension that carries the strong single-start and ordered-reporting guarantees for agents that choose to implement it.
- [ ] Reply to an agent question and request cancellation through A2A when the agent supports them; unsupported controls stay hidden as in 007.
- [ ] Keep every 007 guarantee: consent manifest, recent reauthentication for sensitive operations, sealed secrets, HTTPS and public-address egress, honest state labels, 30/90-day retention, account purge, rollout-OFF semantics.
- [ ] Update the web and iOS surfaces so both show the same projection, including the degraded-mode disclosure.
- [ ] Work with the unmodified Hermes A2A plugin; an upstream patch may be proposed but nothing depends on it.

**Out of scope — explicitly confirmed by the human**

- [ ] BrainBuddy-owned agent hosting, runtime, tools or output verification (unchanged from 007).
- [ ] Multi-agent fan-out, routing and delegation chains.
- [ ] OAuth2 and mTLS authentication schemes and verification of signed Agent Cards (deferred, not rejected).
- [ ] Marketplace, billing, quotas.
- [ ] Automatic Task completion or verification of the agent's result (unchanged from 007).
- [ ] Keeping the bespoke envelope alongside A2A.
- [ ] Exposing BrainBuddy itself as an A2A server with an Agent Card.
- [ ] Changes to voice capture, capture review, Weekly Review or the canonical Task lifecycle.
- [ ] Depending on a Hermes patch or configuration change.

<!--
  Non-goals are the highest-value answers in the interview. Both lists were
  read back and confirmed; record the confirmation, not an assumption.
-->

**Confirmed by**: Max on 2026-09-03

**3. Business objective and KPI**

| metric | baseline today | target | by when |
|---|---|---|---|
| Stock A2A agents reachable end-to-end without agent-side modification | 0 | 2: the unmodified Hermes A2A plugin and an official a2a-sdk sample agent | no calendar deadline; medium appetite (weeks) |
| External starts created per confirmed hand-off across three identical replays | no connector exists | exactly 1 with both stock agents | same |
| 007 security suite (the SC-003 rejection cases) passing on the A2A wire | 100% on the bespoke wire | 100% on the A2A wire | same |
| Task surfaces showing one server projection of a run | web and iOS on the bespoke wire | web and iOS on the A2A wire | same |

These become the `SC-###` success criteria. An objective with no number
produces a spec the acceptance auditor cannot grade.


### 3-4. What was specified and designed

**Spec**: `014-a2a-relay-wire-contract/spec.md` — 27 requirements (FR-001, FR-002, FR-003, FR-004, FR-005, FR-006, FR-007, FR-008, FR-009, FR-010, FR-011, FR-012, FR-013, FR-014, FR-015, FR-016, FR-017, SC-001, SC-002, SC-003, SC-004, SC-005, SC-006, SC-007, SC-008, SC-009, SC-010)

**Design**: `014-a2a-relay-wire-contract/design.md` — 6 screen/state ids (D-01, D-02, D-03, M-01, M-02, M-03)

### Technical plan

`plan.md` is present (179 lines).


### 5. What review found

**Verdict**: `founder-accepted` — degraded panel, single-provider panel, correlated oracles  
**Risk**: `high`  
**Summary**: `.specify/workflows/runs/014-campaign-2/planning-review-summary.json`

| reviewer | verdict | summary |
|---|---|---|
| `requirements-consistency` | changes-required | Feature 014's requirements are broadly self-consistent — all 17 FRs, 10 SCs and 29 ACs are covered end-to-end by plan.md's implementation sections and design.md's 127 states, and the core wire-contract behavior (dispatch states, dedup, observation mapping, retention tiers) is described identically across spec.md, research.md, data-model.md and the four contracts. The review found four important documentation-consistency defects — a self-contradictory account of which 007 requirements are superseded vs. still cited as binding, a design.md 'open decision' note that contradicts the product owner's already-recorded 2026-09-04 resolution, a stale 'pending re-acknowledgement' claim in plan.md that design.md's own header shows was closed, and a connection-test 'rate limited' outcome that SC-009 and the wire contract require but FR-002 and design.md's D-01 state table never define — plus two advisory traceability gaps (quickstart.md silent on SC-004/SC-005, and a miscited FR-017 reference on loading-state design rows). None of these require product input: each has a concrete, citable fix the spec/plan authors or the implementing agent can make directly. |
| `architecture-consistency` | changes-required | Unusually well-grounded plan: I spot-checked ~30 repository claims (service.py 1506/1527/1529-1530, repository.py 91/126-160/284-331/998-1042/1185-1263, config.py 482/651/704-711, container.py 402-427, main.py 195-238, middleware.py path logging, rate_limit.py non-eviction, logging.py uvicorn.access, schemas/common.py extra="ignore", headers.py reserved set, deploy/nginx/default.conf having no access_log, .pre-commit-config.yaml having no exclude, pyproject mutmut/importlinter/ruff scope, Makefile lint scoping, coverage floors, eas.json, .gitleaks.toml 57-64, charter structure, design.md's 127 state ids across six screens) and every one held. Layering and data ownership are respected: the relay stays inside app/modules/agents, Tasks are read through the narrow TaskSnapshot port, and the observer/service seam (scheduling+I/O vs. run-state rules under command_lock with version CAS) is a sound answer to the background-write problem. Five substantive defects remain. One is a false claim about the deployed topology: the push-token redaction layer in deploy/nginx/default.conf is on the frontend Fly image, while the callback origin is BRAIN_BUDDY_PUBLIC_BASE_URL = the backend app's own origin, so push traffic never traverses that nginx. Four more are failure-path and contract gaps: the 007 read-time content projection is not extended to 014's new content fields; the reply-pending observation suspension has no exit condition and can deadlock; a restart while an exchange is still queued is reported as Delivery unconfirmed though nothing was sent (and is unrecoverable on a correlation-ID-ignoring connection); and check-delivery is allowed while the rollout flag is OFF even though it can resend task content. |
| `testability-evidence` | changes-required | Feature 014's test-evidence discipline is unusually rigorous: nearly every AC, FR and SC in spec.md is tied by name to a specific pytest module or test function in research.md Decision J and re-demonstrated as a runnable, mostly-automated scenario in quickstart.md (each with an "Automated equivalent" pytest invocation alongside the manual walkthrough), and the one genuinely manual, money-spending step (the attended Hermes live run, §8) is correctly gated exactly like the existing /verify-live pattern. Verified against the live repository, the plan's factual claims about existing gaps it intends to fix (no EPIC_AGENT_RELAY taxonomy entry today, app/modules/agents absent from both mutation tiers) are accurate. Three gaps remain that bear directly on evidence proportionality and task-lane independence: (1) the plan's own tasks.md-generation convention asserts the removal/migration lane and part of the vendoring lane are parallel-eligible against the backend a2a/observer lane, but the plan's own file-level deltas (Decision F, Project Structure) show both lanes edit the same five backend files, and the vendoring lane's reference-runtime tests functionally require the backend lane to be finished first; (2) the two new highest-risk pure-function modules (a2a/mapping.py, a2a/card.py) are left out of even the zero-cost OBSERVED mutation tier with no tracked follow-up; (3) SC-004 and SC-005 are the only two success criteria never cited by a named test or quickstart scenario, unlike every other SC. None of these require product-owner input — all are testing-strategy and task-sequencing calls for the implementing agent. |
| `privacy-consent-security` | changes-required | Feature 014 is unusually strong on this lens. Purge coverage is real and verified in code (`AgentRepository.delete_all_for_owner` is already called by `AccountService._purge`, backend/app/services/account_service.py:457), export disposition is stated with named exclusions (data-model.md §8) and the currently-undocumented `relay/relay.json` member (account_service.py:277-278) is finally documented by plan §7. Retention is tiered and each tier names its sweep; the one thing that survives purge by design — the agent-side copy of the push callback URL — is stated and justified (push-callback.md, Deregistration). The push token is handled with genuine care: stored only as a keyed fingerprint, kept out of logs by three independent layers (middleware sanitiser, `uvicorn.access` filter, nginx `access_log off` — I confirmed deploy/nginx/default.conf has no `access_log` directive today, so the block is needed and longest-prefix matching makes it work), with a named SC-009 log-scan test and a disclosed platform-edge residual. Constitution Principle IV is explicitly honoured: the outbound `X-Correlation-ID` is server-minted per call and the `contextId` is never derived from an inbound header (a2a-wire.md:41-49). Owner scoping and 404-for-wrong-owner are carried forward (api-deltas.md:5), matching docs/auth.md:23. Three gaps block. First, the resend branch of `check-delivery`/replayed confirmation is a content-bearing egress with no stated connection preconditions — it is not refused on card drift, a reset `scope_verified_at`, a stale connection or an expired content tier, and it is explicitly allowed while the rollout killswitch is OFF. Second, plan.md:174 says restart recovery "adopts or resends", contradicting research.md Decision B and data-model.md, which would license an unattended background send of task content. Third, the discovered agent card is a new untrusted-text surface (name, description, skills, `interface_url`, error-detail values) that no artifact requires to render inertly, even though the same feature just decided agent-reported addresses are never clickable. No product decision is open: the card-metadata retention question was settled by the product owner on 2026-09-04 and is recorded in spec.md, FR-016, AC-024, SC-007, data-model §8 and research Resolved 7 — only design.md's trailing paragraph still calls it open. |
| `ux-accessibility-mobile` | changes-required | Feature 014's UX artifacts are unusually thorough: all required states (loading/empty/error/partial-failure/offline) are named for every one of 127 D-0x/M-0x states, the Keyboard-and-focus and Mobile-viability sections address every rubric point explicitly, ADR-0006 vocabulary is verified clean (zero occurrences of the forbidden term across design.md and all six HTML mockups), no placeholder copy exists, and every affordance traces to an FR with no orphaned affordances or unimplementable requirements. Direct inspection of the six design/*.html mockups against design.md's prose surfaced two concrete, verifiable gaps: the disconnect confirmation's 'what is destroyed' copy omits the now-settled fact that the discovered agent-card metadata is erased with the credential (and design.md's own closing note is stale on this point), and the mobile (M-01/M-02/M-03) mockups mark disabled controls with opacity alone in three separate matched state-pairs where the identical desktop states correctly use the accessible aria-disabled attribute, breaking the design's own claimed web/iOS parity. Two low-priority advisory notes (loading-delay threshold, unsaved add-agent-form navigation) round out the review. Both important findings are directly fixable by the implementing/design agent using facts already settled in spec.md/data-model.md/research.md or an already-established desktop pattern — no new product judgment is required. |
| `adversarial-high-risk` | changes-required | The 014 planning set is unusually thorough on consent, egress pinning, push verification, honesty of labels and purge safety, and most factual claims about the repository check out (process-wide command lock, 007 dispatch pattern, InMemoryRateLimiter never evicting, middleware logging request.url.path, nginx having no access_log directive, StorageBaseModel extra=ignore, AgentRunManifest requiring the bespoke reporting keys, the extension identifier's repository being publicly reachable). Five points need changes before implementation, all settleable by the implementing agent: (1) the rollback story is factually wrong in a way that would silently stall 30-day content expiry for every owner and break the always-on GDPR export for any owner with a 014 run, because the 007 image's expire_due_content parses each run row before nulling it inside one all-or-nothing transaction; (2) outbound requests have no absolute deadline, so one slow-drip agent can pin the shared eight-worker exchange pool (and the observation pool) indefinitely across tenants; (3) CancelTask is queued behind the very SendMessage exchanges it is meant to end; (4) the resend paths (check-delivery, replayed confirmation, restart recovery) sit outside the SC-008 barrier, lack the run_content_expired guard the manifest nulling makes necessary, and bypass the reauthentication trigger 014 FR-004 inherits verbatim; (5) the iOS compatibility claim cites only the two internal EAS profiles while a production App Store profile and submission runbook exist, with no enforcement of the stated minimum build. Five advisory items cover a vacuous SC-007 assertion (context_id equals the never-deleted run id), push audit misclassification after terminal, undefined reply-exchange recovery after a deploy restart, an unconditional credential-destroying migration on an unverified premise, and stale open-decision text in design.md. No product decision is required: every open point is a technical mechanism choice, and the product owner has already settled retention, tiering, result links and rollout semantics in the spec Clarifications. |

All five standard lenses ran.

**Lenses that ran on a fallback oracle**: `requirements-consistency`, `testability-evidence`. These lenses produced a review, but not from the oracle they are configured for. The same verdict from this panel is a weaker result than it would be from a clean one.

- `requirements-consistency`: configured `codex/gpt-5.6-sol`, actually ran `claude/sonnet` — the codex CLI is not installed
- `testability-evidence`: configured `codex/gpt-5.6-sol`, actually ran `claude/sonnet` — the codex CLI is not installed

**The panel collapsed to a single provider.** Every lens whose provenance is known ran on one vendor, so its agreement is one provider's opinion counted several times, not several independent ones.

**Panel correlated**: yes — one oracle holds a strict majority of the lenses whose provenance is known.

**Panel oracles**: `claude/fable` x1, `claude/opus` x2, `claude/sonnet` x3

**Panel providers**: `claude` x6

**Findings**: 16 advisory, 1 blocking, 20 important

**Next action for the architect**: Closed by founder acceptance on 2026-09-04 (ADR-0011 rule 7) after the two-campaign cap: the automated verdict of this run was technical-changes-required; every finding was fixed in the artifacts after the campaign and the product owner accepted the residual risk with the full record in founder_acceptance. Proceed: finalize tasks.md, run /speckit-analyze, and begin implementation in an isolated worktree. requirements-consistency, testability-evidence ran on a fallback oracle because the configured runtime was unavailable. Those lenses are less independent than configured; treat their agreement with the other Claude lenses as weaker corroboration than it looks.

**Closed by founder acceptance.** This is not an approval; the full record follows.

```json
{
  "accepted_by": "Max (maksim.v.kravchuk@gmail.com), product owner and founder",
  "accepted_on": "2026-09-04",
  "campaign_history": [
    {
      "run_id": "014-campaign-1",
      "status": "product-decision-required"
    },
    {
      "run_id": "014-campaign-2",
      "status": "technical-changes-required"
    }
  ],
  "compensating_measures": [
    "Every blocking, important and advisory finding from both campaigns is fixed in the planning artifacts and traceable through the campaign summaries kept under .specify/workflows/runs/014-campaign-1 and .specify/workflows/runs/014-campaign-2, with the fix commits recorded in specs/014-a2a-relay-wire-contract/review.md.",
    "The feature ships behind the external_agent_relay flag, OFF by default, with the release-runbook gate that forbids turning it ON for any user before the refreshed iOS build is the only distributed build.",
    "The acceptance-auditor stage must trace every FR, SC and AC of spec.md to a named executable test before the feature is reported, and the delivery-verifier chain must be green on the implementation worktree.",
    "The attended Hermes live run in docs/external-agent-relay-release.md remains a mandatory, human-run release step before rollout; it is never run in CI or by a subagent.",
    "The ASK review of PR #192 by the repository owner remains the landing gate; this acceptance authorises implementation, not landing."
  ],
  "expires_on": "2026-12-31",
  "rationale": "Feature 014 reached the ADR-0011 hard cap of two review campaigns without an automated approved verdict. Campaign 014-campaign-1 (six lenses; the adversarial lens rerun after a 900 s timeout) returned product-decision-required with four blocking and twenty-one important findings; both product decisions were answered by the product owner on 2026-09-04 and every blocking and important finding was fixed in the planning artifacts before campaign 2. Campaign 014-campaign-2 (six lenses; the adversarial lens rerun after a second timeout under a 1800 s cap) returned technical-changes-required with one blocking, twenty important and sixteen advisory findings and no product decision. Every campaign-2 finding, advisory ones included, was fixed in the artifacts after the campaign, so no reviewed defect is carried into implementation. The residual risk accepted is that the fixed artifacts were not re-reviewed by a third automated campaign, which ADR-0011 forbids, and that two lenses ran degraded on claude/sonnet because the codex CLI is unavailable in this environment, so the panel is a single-provider panel."
}
```


### 6. Task decomposition

`tasks.md` is present (448 lines).


### 7-8. What was built and verified

**Built.** 156 commits on `feat/014-a2a-relay-wire-contract` before this report (four
implementer runs, a documentation lane, three CI-driven fix rounds, the acceptance files,
two review rounds — six findings on PR #192, fourteen on the stacked PRs — fixed in
separate lanes and merged, the re-graded acceptance, and `main` merged in three times, the last at `f6566c8`).
Against `main` the change is 170 files, +50,203 / −10,757 lines. All 131 tasks in
`tasks.md` are ticked; the writer receipt of T131 (ADR-0008, local and uncommitted) was
written for `e40fe98` and re-issued for the final SHA named in section 10.

What landed, in one paragraph: the bespoke feature-007 relay wire (signing secrets, the
event-ingest route, the envelope, the connector) is gone; connections discover an A2A v1.0
agent card, hand a task off as one `SendMessage` under an idempotent manifest token, observe
the agent's Task on a bounded schedule with an optional push callback, reply and cancel over
the same conversation, and disconnect erasing card and credential together; web and iOS
render one server projection with fifteen honest state labels; the official `a2a-sdk`
helloworld sample and the unmodified Hermes A2A plugin are vendored as reference runtimes,
exercised by backend conformance suites and by four Playwright stories against a Compose
stack.

**Deviations from the plan**, every one recorded with its reason in `review.md` under
*Implementation deviations (recorded by the implementer)* and corrected in the contract text
in the same commit: (a) the per-run push token is derived (keyed HMAC), never stored;
(b) a reply runs on the calling request thread while remaining a durable `reply` exchange;
(c) iOS "Copy link" uses React Native's core `Clipboard` behind one adapter file;
(d) two feature-007 link tests were re-targeted to the product decision that every reported
link is inert; (e) `observation_interval` lives on `ExchangePolicy` and
`AgentPrimaryStateLabel` is declared once in `domain.py`; (f) removal tasks T110/T114 and
T115 landed together for import-order reasons, and the reference runtimes found two contract
details (A2A 1.0 returns the Task as the `GetTask` result itself; a card with no security
scheme is an agent asking for no authentication); (g) observed behaviours asserted as
observed — the helloworld sample refuses cancel with `-32002`, and a token-secured Hermes
refuses to push to a private callback, so the loopback conformance proves the scheduled
fallback. After the first real runs of the Playwright stories, one product defect was found
and fixed with tests: a run's `revision` advanced on every observation pass, so a reply typed
while the observer refreshed contact was refused as "changed elsewhere"; `revision` now
advances only when an observation changed something the owner could have been answering.
The first review round added (h) a delivery-check lookup with three outcomes — adopted
task, confirmed empty, unanswered — of which only a confirmed-empty answer licenses a
resend, with one new audit action `delivery_lookup_failed`; (i) an optional
`expected_revision` on `check-delivery`, answered 409 before the lookup, sent by both
clients; (j) `check-delivery` reserving and replaying its `Idempotency-Key`. The second
round added (k) the plan's send invariant restated as *initiation*, not execution;
(l) `card.interface_url` stated to be a validated interface only, never card prose;
(m) a reply's command row and `reply_pending_command_id` made durable before the send, so
a restart mid-reply reconciles the reply from the task's own history and never takes the
start's delivery back; (n) restart recovery split in two steps, only the first — marking,
under the lock, no network — allowed before the app serves; (o) the card's
`securityRequirements` deciding whether the credential may be sent at all; (p) a run's
`push_token_fingerprint` holding its key against retirement like every other live sealed
reference; (q) the hand-off retry gated on the rollout flag on the web as well as iOS;
(r) the delivery check named in the client parity inventory; (s) the forged-push e2e story
forging against a real run and holding its facts across two observation intervals, with
the helloworld gap it leaves stated.

**Two review rounds.** Max asked Codex to review PR #192 at `f073c7a`; it returned six
findings. Each was verified against the code before it was accepted, and each was real;
each was fixed with a failing test first (`2ed7209` … `91b26b6`: 20 new backend cases, plus
web and iOS cases for the client change):

1. A delivery lookup that did not come back (timeout, JSON-RPC error, unusable payload) was
   indistinguishable from "no task exists", so **Check again** could resend after a failed
   lookup — the duplicate SC-008 forbids. Only a confirmed-empty lookup reaches the resend
   now; an unanswered one leaves the run byte-for-byte as it was, still **Delivery
   unconfirmed** with **Check again** offered, and writes one audit row (deviation (h)).
2. The per-connection exchange bound counted only open exchanges, so concurrent dispatches
   could over-admit queued runs onto the workers; the bound is re-checked at the
   queued → open transition under the command lock, where the count is authoritative.
3. The truncation marker was appended after the field limit, so an over-limit observation
   persisted unvalidated and failed the next validated read; the marker now comes out of the
   field's budget, asserted for every limit.
4. **Check again** on iOS was not tied to the run revision the user saw; `expected_revision`
   on the request, refused with 409 before any lookup (deviation (i)).
5. `check-delivery` demanded an `Idempotency-Key` it never spent, so a retry after a lost
   response could send the start message twice; the key is reserved and replayed
   (deviation (j)).
6. A card fetch that trickled bytes until the absolute egress deadline escaped `fetch_card`
   as a 500; it is now the structured `a2a_unreachable` discovery failure, and every other
   pinned-request call site was checked for the same escape.

When the stacked PRs were marked ready for review, Codex reviewed each slice and returned
fourteen findings (two on the planning slice, one on the vendored runtimes, four on the
backend, seven on web and iOS, one on the e2e stories). Each was verified against the code
first — all fourteen were real, two of them wording and test-shape defects in the planning
artifacts rather than product behaviour — and fixed with a failing test first in two
parallel lanes, backend and clients, then merged (`12fc101` … `a80e985`; 22 new backend
cases, 26 web, 8 iOS):

- **Planning (#203)**: the plan's consent invariant read "no background thread ever emits a
  content-bearing message", which the exchange worker executing a confirmed hand-off makes
  literally false; it now speaks of initiation, in the plan, the research, the data model,
  the wire contract and the docstrings (deviation (k)). T035 asked a rejected `javascript:`
  interface to come back in `card.interface_url`, which the data model defines as the
  validated dispatch target; discovery already refuses such an interface before a summary
  exists (`_ALLOWED_SCHEMES`), so the task text, the test and `data-model.md` §1 were
  corrected and two tests now pin that a rejected interface never reaches the card
  (deviation (l)).
- **Vendored runtimes (#204)**: `PROVENANCE.md` promised `test_vendor_provenance.py`, which
  lived in the backend slice; the stack now carries the test and its taxonomy entry in the
  same slice as the files it hashes.
- **Backend (#205)**: a restart caught mid-*reply* reset the start's delivery to
  `delivery_unconfirmed`, opening the resend path to the start message; recovery now branches
  on the exchange kind and reconciles the reply from the task's own history (deviation (m)).
  The boot-time recovery scan performed one `ListTasks` per interrupted exchange before the
  app could serve `/health`, against a 5-second Fly health check; it now only marks under the
  lock, and the lookups run on the observer's control lane after start (deviation (n)). The
  card's `securityRequirements` were never consulted when the credential scheme was chosen,
  against `a2a-wire.md`; they decide now, conjunctions and undeclared names included
  (deviation (o)). The key-retirement guard did not count runs' push-token fingerprints, so
  retiring the anchor key silently broke every live push in both directions; a live
  fingerprint now holds its key exactly as credentials and receipts do (deviation (p)).
- **Web and iOS (#206)**: the duplicate-risk acknowledgement survived a change of agent on
  both surfaces; `blocked_reason` (the authentication block of D-03-S10 / M-03-S09) was typed
  but rendered nowhere; a refused **Check again** was silent on iOS (the web already carried
  its correlation id, and now has the parity test); the iOS expiry projection spread
  `blocked_reason`, artifact names and media types and `result_availability` past the
  content tier's deadline; the retry of a hand-off that never left was a dead control on
  iOS and a live one on the web while rollout is OFF (deviation (q)); `checkAgentRunDelivery`
  was missing from the client parity inventory (deviation (r)); and both surfaces described
  the *oldest* run as the latest because the API lists runs newest first.
- **E2E (#207)**: the forged-push story never touched a real run (deviation (s)).

Two of the first round's six (1 and 6) were violations of requirements the acceptance had
already marked covered (FR-006/SC-008/AC-027 and FR-002); section 9 carries the auditor's
own account, and its third grade after the second round.

**Verified.** Four layers of evidence for the final tree:

- `delivery-verifier` ran the chain in the worktree on `c14b32d` (before either review);
  the implementers re-ran it on `91b26b6` after the first round and in each lane after the
  second; then `delivery-verifier` ran it once more on the merged tree `5958a4b (then the three stores regenerated on `5d9ed3a` after `main` was merged, and the web suite once more on `a263719` after `main`'s side-sheet change)`, with the
  three Allure stores regenerated from scratch as single clean runs.
- The acceptance-auditor re-ran the tests behind every criterion each round touched.
- CI run 881 (https://github.com/MaksimKravchuk/brain_buddy/actions/runs/33983957610) on
  PR #192's final head `43e180e`, the merge of `91b26b6`: all fourteen jobs green, including
  the Compose Playwright E2E job and the Allure quality gate (`maxFailures: 0`); the first
  build of the stack (runs 886–890 on #203–#207) green on every slice.
- The rebuilt stack's CI on each slice's merge ref is linked from the checks of #203–#207.

| Surface | Result | Coverage (floor) |
|---|---|---|
| Backend (`make ci-backend`) | 2977 passed, 0 failed, 0 skipped, 1 deselected (the sandbox-only fly.dev case, green in CI); ruff/black/mypy/import-linter clean; Allure taxonomy OK (2977) | line 98.59 % (98.47), branch 95.69 % (95.61) |
| Frontend (`make ci-frontend`) | 965 tests passed; lint, tsc, build OK; taxonomy OK (965); store regenerated clean, 0 failed | statements 99.01 / branches 97.88 / functions 98.75 / lines 99.22 (98.76 / 97.77 / 98.64 / 98.84) |
| Mobile (`make ci-mobile`) | 1542 tests passed; 32/32 integration checks; tsc, eslint, expo export OK; taxonomy OK (1542); store regenerated clean, 0 failed | statements 95.73 / branches 90.96 / functions 96.50 / lines 95.76 (94 / 88 / 95 / 94) |
| Reference runtimes | helloworld 5, Hermes 8 conformance cases passed, never skipped | — |
| `check_requirement_coverage.py` | 27/27 requirements traced (17 FR + 10 SC) | — |
| `make validate-ci`, `make check-specs` | pass | — |
| Secret-scan guardrails | 16 of 21 pass here; the 5 `ScanRangeTest` cases fail on this machine's git 2.43 at `main` too (a root commit is invisible to `--diff-filter=tuxdb` there) and pass in CI | — |

**Not run locally, run by CI instead.** `make test-e2e` needs a Docker daemon the sandbox
lacks. CI run 881 executed it on `43e180e`: 40 Playwright tests, 39 passed, 1 pre-existing
skip, including the four relay stories `014-SC-002` … `014-SC-005` against the vendored
runtimes; CI's own freshness (39 executed results), product-story (39 meaningful passing
results over the 6 required stories) and Allure-taxonomy (40) validators passed, and the
Allure quality gate was green. The second round's rewritten security story (41 tests now)
typechecks and collects locally and runs first on the rebuilt stack's top slice. The
Playwright Allure results cannot be pulled into the sandbox (the artifact host is blocked),
so the acceptance audit cites CI's artifact `playwright-allure-results` (run 875, id
9971110271, the first green run of the suite; run 881 uploads it again as id 9974739615).

**Known non-feature failures.** `test_admin_deploy_contract.py::test_ADR_0019_…` fails only
in the sandbox (its subprocess curls fly.dev, which the egress proxy blocks) and passes in
CI. The `TaskListPage` recovery-only Discard test from `main` flakes under full parallel
load (four times across this delivery, green on every rerun with two workers); it touches
nothing in this feature.

**Not measured.** The backend mutation gate did not measure this change: CI's "decide the
enforced mutation scope" step found no enforced-scope module touched (in runs 875, 881 and
the stack's), so the base/head measurement steps were skipped (ADR-0013 keeps mutation
report-only outside that scope).

### 9. Acceptance

**Verdict**: `accept`

Matrix: `014-a2a-relay-wire-contract/traceability.md`

The `acceptance-auditor` (a different agent from the implementer) graded the feature three
times. First on `c14b32d` (the tree of PR head `e5603d7`): **accept** — 64 criteria (17 FR,
10 SC, 37 AC), **64 covered, 0 weak, 0 missing** once the charter rows were corrected (its
first pass had SC-004, SC-005 and SC-006 `weak` because `docs/e2e-acceptance-charter.md`
credited the browser stories with clauses the component and conformance suites prove). Then
on `91b26b6`, after the six fixes of the first review round: **accept**, 64 covered, 0 weak,
0 missing, no status moved, and — in the auditor's own words — two of the six findings were
live violations of criteria it had marked covered (the unanswered-lookup resend,
FR-006/SC-008/AC-027, and the card fetch that outlived its deadline, FR-002), read past in
the first grade and now pinned by tests that fail if reverted; it judged the new
`expected_revision` 409 not a narrowing of FR-006. It also found that the local web and iOS
Allure stores were unions of several runs each carrying one stale `failed` result, and that
`validate_ci_artifacts.py results` does not catch this; the stores were regenerated clean
before being presented again. After the second review round the auditor was not run again: the product owner asked to release without spending another audit run, so the verdict stands at `91b26b6`, the round covered by the verifier's chain on the merged tree and by the tests named in `review.md` (k)–(s), and `acceptance.md` records exactly that. Three findings stay on the record: the loading
states D-01-S02, M-01-S02 and M-03-S20 have no assertion and are left for a human waiver;
the Playwright layer's evidence is CI's artifact, not a local file; and the validator gap is a
follow-up (section 11).

### 10. How it landed

ASK-class change (CI workflow, Compose, delivery scripts, auth-adjacent API modules,
migration), so it lands through reviewed pull requests, never automatic trunk promotion. It
was first opened as one draft PR, https://github.com/MaksimKravchuk/brain_buddy/pull/192 on
`claude/agent-delegation-yv9auj` against `main` (`040f1de`): 170 files, green on its final
head `43e180e` (run 881). At Max's request it was then split into a stack of five reviewable
PRs, each one squashed commit of one layer of paths at their final state, chained bottom-up
under the repository's `stack/**` naming — besides `main`, the only bases for which
`ci.yml` runs pull-request CI. (The stack was briefly opened as #196–#200 under
`claude/014-stack-*`, which got no CI for exactly that reason, and re-opened.) After the
second review round and the move of `main` the stack was rebuilt on `main` at `f6566c8`
from the merged feature head and force-pushed to the same five branches, so each PR stays
one squashed slice:

| Slice | PR | Branch | Head | Base |
|---|---|---|---|---|
| 1 planning artifacts and tooling | #203 | `stack/014-1-planning` | the branch head | `main` |
| 2 vendored reference runtimes, with the provenance test | #204 | `stack/014-2-vendored-runtimes` | the branch head | slice 1 |
| 3 backend | #205 | `stack/014-3-backend` | the branch head | slice 2 |
| 4 web and iOS | #206 | `stack/014-4-clients` | the branch head | slice 3 |
| 5 e2e against the runtimes, delivery record | #207 | `stack/014-5-delivery` | the commit that carries this report | slice 4 |

Slice 1 carries the planning artifacts with no task ticked and no delivery record, so `main`
never records a delivery ahead of the code; slice 5 ticks the tasks and adds
`acceptance.md`, `traceability.md` and this report. The rollout flag `external_agent_relay`
stays OFF, which makes every intermediate merge safe: the web and iOS clients on `main` never
reach the new routes, and both clients travel together in slice 4 because
`contracts/api-client-parity.json` binds them in both directions. The top of the stack
matches the tree of the feature head up to one reformatted test call (so no line carries the
shape the secret scan misreads and no `.gitleaksignore` fingerprint is needed). Merge
bottom → top; after each merge GitHub retargets the next slice onto `main`. PR #192 is
closed in favour of the stack; its branch is kept as the full commit history and the review
thread, and the feature branch is merged into it after this report so that history stays
complete. The pre-freeze writer receipt (ADR-0008) was re-issued for the feature-branch head
that includes this report, validated locally with `scripts/validate_pre_freeze_receipt.py`,
and is deliberately not committed. Independent review of each slice, QA, exact-SHA CI on
each merge, landing, deploy, the attended Hermes live run (`/verify-live` class, never CI,
never an agent) and the rollout of `external_agent_relay` remain post-freeze human
obligations.

### 11. Still open

1. **Attended Hermes live run** — a human release step per `docs/external-agent-relay-release.md`
   §"Hermes live evidence"; never run in CI or by an agent. Not done.
2. **Rollout gate** — `external_agent_relay` must stay OFF for every user until the refreshed
   iOS build is the only distributed build (TestFlight and App Store phased release complete).
   The pre-deploy count of bespoke connection rows and the rollback consequences are in the
   runbook; none has been exercised against production.
3. **ASK review of the CI and Compose changes** — slice 5 (#207): the `e2e` job builds two
   fixture images and publishes the Hermes fixture on a host port; `scripts/run_playwright_e2e.sh`
   turns the rollout flag on through the operator API and sets a 5-second observation
   interval. These are reviewed-PR changes the reviewer should read, not infer.
4. **A product question the second round surfaced** — whether a task that is already
   terminal may still retry a hand-off that never left (deviation (q) gates the retry on the
   rollout flag only and leaves that as it was).
5. **Coverage margin** — backend branch coverage is 95.69 % against a 95.61 % floor;
   a nearly neutral change can tip it. The floor is ratchet-only and was not raised for this
   feature.
6. **Mutation evidence** — no mutation measurement exists for the new relay modules (outside
   the enforced scope); the nightly report-only run is the only signal.
7. **Follow-ups noted, not done**: migrate iOS "Copy link" from React Native's deprecated core
   `Clipboard` to `expo-clipboard` (one file); the backend dependency pin `fastapi>=0.110,<1.0`
   lets CI resolve Starlette 1.x while the lock file holds 0.48 — two tests were rewritten to
   hold on both, but the drift itself is unaddressed; the `TaskListPage` recovery flake on
   `main` is unrelated to this feature and still there; `validate_ci_artifacts.py results`
   passes a store as soon as one fresh executed result exists and ignores stale and `failed`
   entries beside it — CI regenerates its stores, so the gap is local-only, but a local
   evidence bundle can carry a failed result unnoticed, as this one did until the auditor
   read every result file; the forged-push e2e story exercises a helloworld run, which
   declares no push support, so the fingerprint comparison itself is proved by the backend
   route tests rather than end to end (a Hermes-based variant would close that); the
   `scripts.test_secret_scan_guardrails` `ScanRangeTest` cases depend on git ≥ 2.45
   behaviour and fail on older local gits while passing in CI.
8. **Evidence provenance** — the Playwright layer's Allure evidence is CI's artifact, not a
   local file; the acceptance matrix says so per criterion.
