# Tasks: A2A Relay Wire Contract

<!--
  BrainBuddy override: delivery gates. Upstream treats tasks.md as an
  execution script; here it is portable planning input that never bypasses
  isolated worktrees, tests-before-implementation, independent acceptance,
  ADR-0008 landing classification, or CI. Those gates are restated below and
  must survive any upstream refresh.
-->

**Input**: Design documents from `/specs/014-a2a-relay-wire-contract/`

**Prerequisites**: plan.md (required), spec.md (required for user stories), design.md (required when the feature has a user-visible surface), research.md, data-model.md, contracts/

**Delivery gates** (non-negotiable, regardless of who executes these tasks):
isolated worktree and feature branch; failing test written and observed failing
before the implementation that satisfies it; Allure taxonomy on every product
test with the covering feature-qualified id (`NNN-FR-###`/`NNN-SC-###`) in
the test name or story; acceptance
graded by an agent that did not write the code; landing class decided by
`scripts/classify_path_risk.py`, with SHIP and SHOW landing PR-less through
verified trunk and ASK-class changes never landing automatically — a PR carries
their review evidence, but merging it does not by itself update `main` (ADR-0008;
ADR-0008; see `AGENTS.md` and `docs/autonomous-delivery-runbook.md` for the recorded
approval and ruleset requirements).

Before freeze, the implementation writer MUST produce a typed receipt using
`.specify/templates/pre-freeze-receipt.schema.json` and run
`python3 scripts/validate_pre_freeze_receipt.py <receipt> --sha <full-lowercase-sha>`.
<!-- BrainBuddy pre-freeze receipt contract: tasks. Preserve this section. -->
The receipt covers only writer-owned pre-freeze gates. Independent review, QA,
CI, landing, deploy, and production smoke remain post-freeze obligations under
ADR-0008 and must never be represented as writer PASS evidence.

**Tests**: Tests are expected for behavior changes; include backend pytest/FastAPI
TestClient, frontend Vitest/Testing Library, operation-state, or deterministic
repository checks unless the spec explicitly waives tests for a docs/tooling-only
change.

**Organization**: Tasks are grouped by user story to enable independent implementation and testing of each story. Include consent enforcement, mobile/resilience handling, observability (correlation IDs, actionable errors/progress), release/smoke validation, and data-safety safeguards where relevant.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2, US3)
- Include exact file paths in descriptions

## Path Conventions

- **Backend**: `backend/app/` (layered `api/` → `services|modules/` → `repositories`), tests in `backend/tests/`, vendored third-party runtimes in `backend/vendor/` (outside every lint, type and coverage scope)
- **Web**: `frontend/src/` with Vitest suites in `frontend/src/**/__tests__/` and Playwright in `frontend/tests/e2e/`
- **Mobile**: `mobile/src/` with Jest suites in `mobile/src/**/__tests__/` and the real-backend harness in `mobile/integration/`
- **Feature ids**: every task names the feature-qualified requirement ids it serves (`014-FR-###`, `014-SC-###`, `014-AC-###`); every test task names the exact test file and the ids its test names or Allure stories carry, because `scripts/check_requirement_coverage.py` traces only `014-FR-###`/`014-SC-###` (or `014_FR_###`/`014_SC_###`) markers and AC ids go into docstrings as in 007
- **Design ids**: every UI task names the `design.md` state ids (`D-##-S##`, `M-##-S##`) it realises; the acceptance auditor traces criteria through them
- **Lanes** (plan.md "tasks.md convention"): the frontend lane and the mobile lane are `[P]` against the backend a2a/observer lane; the removal/migration pass on `service.py`, `domain.py`, `repository.py`, `headers.py` and `secrets.py` is sequenced after the a2a/observer tasks on the same file and is never `[P]` against them (one ordered pass per file); vendoring plus `test_vendor_provenance.py` is `[P]`, while the two reference-runtime test modules are gated on the whole §1–§4 pipeline. `[P]` is granted conservatively: a task touching a file that more than one story or lane edits (`service.py`, `domain.py`, `repository.py`, `schemas/agents.py`, `api/agents.py`, `observer.py`, `container.py`, `main.py`, the rewritten backend suites, the shared client `types`/`client`/`hooks`/`copy` modules and the run-surface components) is not `[P]`, even inside its own phase
- **Human-only steps that are not tasks**: the attended Hermes live run (`quickstart.md` §8, spends provider money, approval-gated like `verify-live`), turning `external_agent_relay` ON, landing, deploy and the TestFlight/App Store release. They are release steps in `docs/external-agent-relay-release.md`, never work the implementer performs

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Isolated worktree, dependencies, the vendored reference runtimes with their provenance record, tool hygiene for the vendored tree, and the Allure and environment scaffolding every later phase relies on. The `feature-implementer` preconditions apply first: `review.md` / `.specify/workflows/runs/<run-id>/planning-review-summary.json` must read `approved` or `founder-accepted` and `/speckit-analyze` must report zero CRITICAL findings before T001 starts.

- [x] T001 Create the isolated worktree and feature branch per `.claude/agents/feature-implementer.md` — `git worktree add .worktrees/014-a2a-relay-wire-contract -b feat/014-a2a-relay-wire-contract` from the default branch — set distinct `BRAIN_BUDDY_DATA_DIR`, backend/frontend ports, `BRAIN_BUDDY_MOBILE_IT_PORT` and `BRAIN_BUDDY_E2E_PROJECT` and record which were used, then confirm `cd backend && pytest -q`, `make test-frontend` and `make test-mobile` are green on the untouched branch so every later failure is attributable (delivery gate: isolated worktree and attributable green baseline, plan.md "Delivery boundary"; traced as 014-FR-017 because the reference-runtime conformance it demands is what this baseline makes attributable)
- [x] T002 Add the dev-only reference-runtime dependency `a2a-sdk[http-server]==1.1.2` to the `dev` extra in `backend/pyproject.toml`, re-lock `backend/uv.lock` (httpx moves from 0.27.2 to 0.28.x inside the existing `>=0.25,<0.29` range; a2a-sdk never enters the runtime dependency set, research.md Decision A) and run the full backend suite once before any feature work as the re-lock validation over the seven production httpx call sites (`backend/app/modules/agents/egress.py`, `backend/app/ai/title_completion.py`, `backend/app/ai/providers/openai_provider.py`, the three voice adapters `openai_stt.py`, `deepgram_stt.py` and `reconciler.py` under `backend/app/workflows/voice_brain_dump/adapters/`, and `backend/app/modules/agents/connector.py` until T115 deletes it) (dependency gate: the dev-only reference runtime and the httpx re-lock, research.md Decision A; traced as 014-FR-017 because the a2a-sdk sample is the second reference runtime that requirement demands)
- [x] T003 Keep every tool out of the vendored tree and start the mutation calibration clock: add the global `exclude: ^backend/vendor/` to `.pre-commit-config.yaml`, add ruff `extend-exclude = ["vendor"]` under `[tool.ruff]` in `backend/pyproject.toml`, and append `app/modules/agents/a2a/mapping.py` and `app/modules/agents/a2a/card.py` to `[tool.mutmut] only_mutate` in `backend/pyproject.toml` (report-only OBSERVED tier under ADR-0016, blocks nothing; research.md Decisions F and J) (tool-hygiene gate for the vendored tree and the ADR-0016 calibration clock; traced as 014-FR-017, whose vendored runtimes this hygiene exists for — `mapping.py` and `card.py` are the modules under mutation, not requirements this task realises)
- [x] T004 [P] Write the failing provenance test `backend/tests/test_vendor_provenance.py` — `test_014_FR_017_vendored_helloworld_files_match_provenance_sha256` and `test_014_FR_017_vendored_hermes_plugin_files_match_provenance_sha256` assert that every vendored upstream file under `backend/vendor/a2a_helloworld/` and `backend/vendor/hermes_a2a/plugins/platforms/a2a/` hashes to the sha256 recorded in its `PROVENANCE.md` and that the recorded commit and license are present; Allure epic `External agent relay` (014-FR-017, 014-SC-001)
- [x] T005 [P] Vendor the unmodified official a2a-sdk helloworld sample byte-for-byte — `backend/vendor/a2a_helloworld/__main__.py`, `backend/vendor/a2a_helloworld/agent_executor.py`, `backend/vendor/a2a_helloworld/LICENSE` (Apache-2.0) and `backend/vendor/a2a_helloworld/PROVENANCE.md` (upstream commit `6603ba3f2c31a7ef33e70b9d8b5b5f8be42ac9a3`, per-file sha256) — from the a2a-samples checkout research.md names in its Sources; no BrainBuddy edit to any vendored file (014-FR-017, 014-SC-001)
- [x] T006 [P] Vendor the unmodified Hermes A2A plugin byte-for-byte — `backend/vendor/hermes_a2a/plugins/platforms/a2a/__init__.py`, `adapter.py`, `protocol.py`, `security.py`, plus `backend/vendor/hermes_a2a/LICENSE` (MIT) and `backend/vendor/hermes_a2a/PROVENANCE.md` (upstream commit `63279301bcbdc185c1b07b98a9312eb0c862f26d`, per-file sha256) — from the hermes-agent checkout research.md names; T004 turns green once T005 and T006 land (014-FR-017, 014-SC-001)
- [x] T007 [P] Write the BrainBuddy-owned Hermes harness beside the vendored plugin: the `backend/vendor/hermes_a2a/gateway/` stub package providing exactly the imports `adapter.py` needs (`gateway/config.py` with `Platform` carrying `_missing_` and `PlatformConfig(enabled, extra)`; `gateway/platforms/base.py` with `BasePlatformAdapter`, `MessageEvent`, `MessageType`, `ProcessingOutcome`, `SendResult`, `build_source`, the handler and connection hooks research.md Decision G lists) and `backend/vendor/hermes_a2a/run_stub.py`, which builds `A2AAdapter(PlatformConfig(enabled=True, extra={"port": …}))` with a scripted reply handler (echo; `[INPUT_REQUIRED] …` when the text contains `ask me`; `STUB_REPLY_DELAY_SECONDS`), reads `A2A_BEARER_TOKEN`/`A2A_HOST`/`A2A_PORT`/`A2A_PUBLIC_URL`, sets `HOME` to a temp dir, binds port 0 under test and prints the bound port back, and never needs a model (014-FR-017, 014-SC-001, 014-SC-006)
- [x] T008 [P] Add `EPIC_AGENT_RELAY = "External agent relay"` to `backend/tests/allure_taxonomy.py` with `_MODULE_TAXONOMY` entries under it for every new stem (`agent_a2a_card`, `agent_a2a_client`, `agent_a2a_mapping`, `agent_observer`, `agent_repository_migration`, `agent_a2a_extension_identifier`, `vendor_provenance`, `agent_a2a_reference_helloworld`, `agent_a2a_reference_hermes`) and for the rewritten `agent_relay_service` and `agent_relay_api` stems, which today fall back to `EPIC_QUALITY` (014-SC-001, 014-SC-003, 014-SC-009)
- [x] T009 [P] Add the Playwright taxonomy rule for `frontend/tests/e2e/agent-relay.compose.spec.ts` to `frontend/tests/allure.fixtures.ts` so its stories report under the `External agent relay` epic named in quickstart.md §7 (the existing `e2e\/agents` rule does not match `agent-relay`), and mirror it in `mobile/src/test/allureTaxonomy.ts` with a `/features/agents/` and `/agents/` rule under that epic so the SC-004 / SC-005 mobile suites stop falling to the `Mobile / General behaviour` fallback, plus a `/lifecycle/__tests__/agentGuards` rule placed before the existing `/lifecycle/` rule (first match wins in `PATH_RULES`), so T084's `agentGuards.test.ts` lands in the relay epic rather than `Native GTD / Lifecycle guards` (014-SC-004, 014-SC-005)
- [x] T010 [P] Document the whole `agent_relay` block in `.env.example`, the authoritative environment reference, which today names only `BRAIN_BUDDY_AGENT_RETENTION_SWEEP_INTERVAL_SECONDS`: the existing settings `backend/app/core/config.py` already reads — `BRAIN_BUDDY_PUBLIC_BASE_URL` (now also the push-callback origin, `{public_base_url}/api/a2a/push`, the backend app's own origin), `BRAIN_BUDDY_AGENT_STALE_AFTER_SECONDS`, `BRAIN_BUDDY_AGENT_REPORTING_WINDOW_SECONDS`, `BRAIN_BUDDY_AGENT_CONTENT_RETENTION_SECONDS`, `BRAIN_BUDDY_AGENT_CONNECTOR_TIMEOUT_SECONDS`, `BRAIN_BUDDY_AGENT_CONNECTOR_MAX_RESPONSE_BYTES`, `BRAIN_BUDDY_AGENT_ALLOW_PRIVATE_DESTINATIONS` (never in production) — and the eight new names `BRAIN_BUDDY_AGENT_OBSERVATION_INTERVAL_SECONDS`, `BRAIN_BUDDY_AGENT_REPLY_WINDOW_SECONDS`, `BRAIN_BUDDY_AGENT_DISPATCH_WAIT_SECONDS`, `BRAIN_BUDDY_AGENT_EXCHANGE_WORKERS`, `BRAIN_BUDDY_AGENT_MAX_EXCHANGES_PER_CONNECTION`, `BRAIN_BUDDY_AGENT_OBSERVER_WORKERS`, `BRAIN_BUDDY_AGENT_CONTROL_WORKERS`, `BRAIN_BUDDY_AGENT_A2A_TASK_MAX_RESPONSE_BYTES`, each with the default and bounds of data-model.md §9; state that no bespoke `/agent-events` callback variable exists any more, and note the rollout gate pointer to `docs/external-agent-relay-release.md`; verified by `test_014_FR_008_env_example_documents_every_agent_relay_variable` in `backend/tests/test_config.py` (T011), which reads `.env.example` and asserts that every `BRAIN_BUDDY_AGENT_*` name `config.py` reads, plus `BRAIN_BUDDY_PUBLIC_BASE_URL`, appears in it (the repository-contract pattern of `backend/tests/test_admin_deploy_contract.py`) (014-FR-007, 014-FR-008, 014-FR-016)

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: The A2A protocol surface (`types`, `card`, `client`, `mapping`), the egress absolute deadline, the bounded push limiter and log redaction, the configuration settings, the repository columns with the ledgered migration, the frozen-007 rollback compatibility, the service/container ports and the published extension specification. Every user story depends on these.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete. Failing tests and the fake agents first (T011–T022; T015 provides the fakes T018 imports), then implementation (T023–T033).

- [x] T011 [P] Write failing tests in `backend/tests/test_config.py` — `test_014_FR_008_agent_relay_settings_expose_observer_pools_and_bounds` (the eight new `AgentRelaySettings` fields with the defaults and bounds of data-model.md §9, `max_exchanges_per_connection ≤ exchange_workers`, `a2a_task_max_response_bytes` hard maximum 1 MiB) and `test_014_FR_007_push_base_url_is_public_base_url_plus_api_prefix_and_a2a_push` (`AGENT_PUSH_PATH = "/a2a/push"`, `agent_relay_push_base_url = {public_base_url}{api_prefix}/a2a/push`, production canonical-HTTPS validation unchanged) and `test_014_FR_008_env_example_documents_every_agent_relay_variable` (the T010 verification: every `BRAIN_BUDDY_AGENT_*` name `config.py` reads, plus `BRAIN_BUDDY_PUBLIC_BASE_URL`, appears in `.env.example`); the 007 callback-URL cases at lines 87–115 are deleted by the removal task T109 (014-FR-007, 014-FR-008)
- [x] T012 Implement the settings in `backend/app/core/config.py`: `AgentRelaySettings` gains `observation_interval_seconds`, `reply_window_seconds`, `dispatch_wait_seconds`, `exchange_workers`, `max_exchanges_per_connection`, `observer_workers`, `control_workers`, `a2a_task_max_response_bytes` (env names and bounds per data-model.md §9), the constant `AGENT_PUSH_PATH` and the derived `agent_relay_push_base_url`; `AGENT_EVENTS_PATH` and `agent_relay_callback_url` stay until T115 deletes them together with their last caller (014-FR-007, 014-FR-008)
- [x] T013 [P] Write the failing card suite `backend/tests/test_agent_a2a_card.py` (names carry `014_FR_001`, `014_FR_002`, `014_FR_003`, `014_FR_011`; AC-001..AC-005 and AC-037 in docstrings): both the v1.0 card and the legacy Hermes shape parse with unknown fields ignored; the first `JSONRPC` interface whose `protocolVersion` matches `^1\.0(\.\d+)?$` is selected and its host validated separately under 007 FR-004; scheme mapping to `bearer`/`api_key`/unsupported naming the scheme; an API-key header colliding with the reserved set or not an HTTP field-name token yields `a2a_auth_scheme_unsupported`; a `required: true` extension BrainBuddy does not implement yields `a2a_no_supported_interface`; the FR-011 URI in `capabilities.extensions[].uri` sets `guaranteed`, otherwise `best_effort`; `card_fingerprint` is sha256 over the canonical drift subset of data-model.md §1; `AgentCardSummary` bounds (`MAX_SKILLS` 50, `MAX_SKILL_TEXT_CHARS` 500, `MAX_CARD_DESCRIPTION_CHARS` 2000, `MAX_EXTENSIONS` 20); failure categories `a2a_unreachable`, `a2a_not_an_agent`, `a2a_no_supported_interface`, `a2a_protocol_version_unsupported` with the closed `last_test_error_detail` shapes (014-FR-001, 014-FR-002, 014-FR-003, 014-FR-011)
- [x] T014 [P] Write the failing pure mapping suite `backend/tests/test_agent_a2a_mapping.py` (`014_FR_009`, `014_FR_013`; AC-013, AC-016, AC-017, AC-020 in docstrings): `pytest.mark.parametrize` over every `TaskState` — `SUBMITTED→accepted`, `WORKING→running` with status text as progress, `INPUT_REQUIRED→blocked` with the question, `AUTH_REQUIRED→blocked` with the fixed `blocked_reason` "Agent needs additional authentication" and `needs_user=False`, `COMPLETED→completed` with text artifacts joined then the final status text, `FAILED→failed` with the reason, `REJECTED→failed` with "Rejected by agent" prefix, `CANCELED→cancelled`, `UNSPECIFIED` contact only, a direct `Message` → completed with its text; `result_link` = first `Part.url` with `result_link_interactive` always false; non-text parts → `artifacts_summary` placeholders (≤20); truncation at `MAX_PROGRESS_CHARS`/`MAX_QUESTION_CHARS`/`MAX_RESULT_CHARS` appends " … [truncated]" and still accepts; BrainBuddy-assigned versions and terminal locking inputs (014-FR-009, 014-FR-013)
- [x] T015 [P] Create the loopback fake agents `backend/tests/a2a_fakes.py` (`ThreadingHTTPServer`, pattern of the Hermes plugin's own tests) before the suites that import them (T018 first): the guaranteed-tier fake that declares the extension, asserts `A2A-Extensions` on the first request and refuses to dedup when it is absent, dedups by `(contextId, messageId)` and can be configured to answer `ListTasks` empty; a Hermes-shaped and a helloworld-shaped fake (blocking send, new task per message, no dedup); a drip-feeding agent emitting one byte per interval; the `BlockingA2AAgent` barrier port for the SC-008 tests; a recording fake that counts every content-bearing `SendMessage` for `test_no_background_thread_ever_sends_content`; and a fake that returns a task with a foreign `contextId` (014-SC-002, 014-SC-008, 014-FR-006, 014-FR-007)
- [x] T016 [P] Extend `backend/tests/test_agent_egress.py` with failing deadline tests (`014_FR_007`, `014_FR_008`; AC-034 in docstrings): `pinned_request(deadline_seconds=…)` raises `EgressDeadlineExceeded` while still waiting for headers and inside the `iter_raw` loop, closes the stream on breach, leaves the per-chunk `httpx.Timeout` behaviour intact, and `interactive_result_link` still returns `False` for every link with its documented reason; move the egress-bound body-cap cases out of `backend/tests/test_agent_connector.py` into this module (that file is deleted in T109) (014-FR-007, 014-FR-008, 014-FR-016)
- [x] T017 [P] Write the failing identifier test `backend/tests/test_agent_a2a_extension_identifier.py` (`014_FR_011`): `docs/a2a-extensions/single-start/v1.md` exists in the repository at the frozen path, and the served `tier_disclosure_url` on a connection response equals the frozen identifier `https://github.com/MaksimKravchuk/brain_buddy/blob/main/docs/a2a-extensions/single-start/v1.md`; the file case turns green with T033, the served-URL case with T045 (014-FR-011)
- [x] T018 Write the failing client suite `backend/tests/test_agent_a2a_client.py` over `httpx.MockTransport` and the loopback fakes of T015 (after T015, since the suite imports them; `014_FR_004`, `014_FR_006`, `014_FR_008`, `014_SC_003`, `014_SC_009`): every call goes through `validate_destination` + `pinned_request` (one pinned address, no redirects, HTTPS unless the governed private class); the request envelope of `contracts/a2a-wire.md` (`A2A-Version: 1.0`, a server-minted `X-Correlation-ID` per call — `test_014_SC_009_adversarial_inbound_correlation_id_never_reaches_the_outbound_request` proves an oversized or adversarial inbound header is never forwarded —, `A2A-Extensions` plus `message.extensions` for the guaranteed tier, proto-defined params only, `tenant` echoed when set); bounded bodies including the larger task-read cap and the over-cap `GetTask` → `ListTasks {includeArtifacts:false}` fallback in which an over-cap completed task still reaches its terminal state with `result_availability=too_large`; the full error-mapping table (`-32001`…`-32009`, `-32600/-32602/-32700`, `-32601`, `-32603`, HTTP 401/403/429/5xx, timeout, transport, `a2a_response_invalid`, `a2a_response_over_cap`); `test_drip_feeding_agent_trips_the_absolute_deadline` (one byte per interval past the window raises `EgressDeadlineExceeded` and closes the stream, an agent answering at exactly the window is still received); `SendMessage`, `GetTask`, `ListTasks`, `CancelTask`, `CreateTaskPushNotificationConfig` request shapes; structured log fields limited to the Decision K allowlist (014-FR-004, 014-FR-006, 014-FR-008, 014-SC-003, 014-SC-009)
- [x] T019 Write the failing push-infrastructure tests in `backend/tests/test_agent_relay_api.py` (the first pass on this rewritten suite, never `[P]`; `014_FR_008`, `014_SC_009`): a bounded per-key limiter with `max_keys` that evicts a run's bucket on terminal or disconnect and never grows past the cap, while `InMemoryRateLimiter` is untouched; the pure path sanitiser rewrites any path under `{api_prefix}/a2a/push/` to `{api_prefix}/a2a/push/{run_id}/[redacted]` on both the `api_request` and `api_request_failed` lines and never raises on a malformed path; the `uvicorn.access` filter rewrites or drops access lines whose path starts with that prefix (014-FR-008, 014-SC-009, 014-FR-016)
- [x] T020 [P] Write the failing SC-010 migration suite `backend/tests/test_agent_repository_migration.py` (`014_FR_012`, `014_SC_010`): a seeded pre-014 bespoke connection row (no `wire` key) becomes `status=disconnected`, `disconnect_reason=superseded_wire_contract`, `credential=None` with `inbound_secret` dropped after the ledgered migration `a2a_wire_contract_v1`; the rewrite is counted on the ledger row and audited with one `wire_superseded` row per rewritten connection; a second startup rewrites nothing; `test_wire_less_rows_are_rewritten_on_every_startup` (a wire-less row inserted between two startups is rewritten on the next one, outside the ledger); its dispatched non-terminal runs are stamped `connection_disconnected_at` and keep bounded history; a hand-off preview on it is refused with `superseded_wire_contract`; `POST /api/agent-events` and `POST /api/agent-connections/{id}/signing-secret` answer 404; importing `app.modules.agents.connector` raises `ModuleNotFoundError` — the route, preview-refusal and import-absence cases turn green with the removal tasks T110–T115 (014-FR-012, 014-SC-010)
- [x] T021 [P] Write failing repository tests in `backend/tests/test_agent_repository.py` (`014_FR_008`, `014_FR_016`, `014_SC_007`): the new run columns and indices (`agent_task_id`, `context_id`, `next_observation_at`, `exchange_state`, `identifiers_expire_at`) added by the ledgered migration; the conditional `UPDATE … WHERE owner_id=? AND id=? AND run_version=?` returns no-op on a stale `run_version`; the due-observation query selects dispatched, non-terminal, non-disconnected runs with `next_observation_at <= now` across owners; there is no run insert path callable from background threads; `export_owner_data` excludes `credential`, `push_token_fingerprint` and `card_fingerprint`; `delete_all_for_owner` covers every table for the owner only (014-FR-008, 014-FR-016, 014-SC-007)
- [x] T022 Write the failing rollback-boundary tests in `backend/tests/test_agent_relay_service.py` (the first pass on this rewritten suite, never `[P]`; `014_FR_012`, `014_SC_010`): `test_frozen_007_connection_document_validates_a_014_payload` (storage keeps `endpoint_url`, a bearer connection stores no `auth_header_name` key, `StorageBaseModel` ignores unknown fields) and `test_frozen_007_run_event_and_command_documents_validate_014_payloads` (the 014 manifest carries the inert `reporting = {callback_url: "", connection_id}` plus 007 defaults and `reporting_instructions = ""` and `instructions_version` so a frozen copy of the 007 `AgentRunManifest`, event and command documents validate 014 rows) (014-FR-012, 014-SC-010)
- [x] T023 Create the package `backend/app/modules/agents/a2a/__init__.py` and `backend/app/modules/agents/a2a/types.py`: strict Pydantic models on `StrictBaseModel` (`backend/app/schemas/common.py`) for the A2A subset — agent card in both accepted shapes (unknown fields ignored), `Task`, `TaskState`, `Message`, `Part` (text, url, file/data placeholders), `TaskPushNotificationConfig`, the JSON-RPC 2.0 request/response envelope with exactly one of `result`/`error`, and the allowlisted error codes of `contracts/a2a-wire.md` (014-FR-001, 014-FR-009)
- [x] T024 Implement `backend/app/modules/agents/a2a/card.py` to make T013 pass: fetch `{origin}/.well-known/agent-card.json` (or the full card URL when the address ends with `agent-card.json`) through `pinned_request` with `Accept: application/json` and no credential, parse, select the interface, validate its host separately, map security schemes, detect the FR-011 extension for the tier, compute `card_fingerprint`, and produce the bounded `AgentCardSummary` and the failure category (014-FR-001, 014-FR-002, 014-FR-003, 014-FR-011)
- [x] T025 Implement the absolute wall-clock deadline in `backend/app/modules/agents/egress.py` to make T016 pass: `pinned_request(deadline_seconds=…)` enforced while waiting for headers and inside the `iter_raw` loop, closing the stream and raising the new `EgressDeadlineExceeded`; pinning, redirect refusal, body bounds and `interactive_result_link` unchanged (014-FR-007, 014-FR-008, 014-FR-016)
- [x] T026 Implement `backend/app/modules/agents/a2a/client.py` to make T018 pass: the `A2AClientPort` protocol and `A2AClient` — `SendMessage` (exchange timeout `httpx.Timeout(connect=5, read=reply_window_seconds + 15)` under a deadline of the same size), `GetTask`, `ListTasks`, `CancelTask`, `CreateTaskPushNotificationConfig` (each under an absolute deadline of `connector_timeout_seconds` (10 s) + 5 s, data-model.md §9) over `egress.pinned_request`; per-call server-minted `X-Correlation-ID` (`uuid4().hex`, `obs_<id>` for scheduled observations) never derived from an inbound header; the two byte caps with the over-cap `GetTask` → `ListTasks` fallback; the error mapping of `contracts/a2a-wire.md`; structured logs carrying only method, HTTP status class, allowlisted A2A code, `response_over_cap`, protocol version, duration and ids (014-FR-004, 014-FR-006, 014-FR-008, 014-SC-009)
- [x] T027 Implement the pure projection `project_observation(task_or_message, *, now, limits) -> Observation` in `backend/app/modules/agents/a2a/mapping.py` to make T014 pass (014-FR-009, 014-FR-013)
- [x] T028 Implement the bounded per-key limiter in `backend/app/core/rate_limit.py` (a `max_keys` variant with `evict(key)`; the process-wide fixed-size route counter and the unknown-id rejection counter beside it) to make the limiter cases of T019 pass, leaving `InMemoryRateLimiter` untouched (014-FR-008, 014-SC-003)
- [x] T029 Implement the two in-process redaction edges to make the remaining T019 cases pass: the pure path sanitiser applied before every log call in `CorrelationIdMiddleware` (`backend/app/api/middleware.py`) and the `uvicorn.access` logging filter in `backend/app/core/logging.py` that rewrites or drops lines whose path starts with `{api_prefix}/a2a/push/` (014-FR-016, 014-SC-009)
- [x] T030 Implement the schema and migration in `backend/app/modules/agents/repository.py` to make T020 (its repository cases) and T021 pass: the new run columns and indices, the ledgered migration `a2a_wire_contract_v1` in `_initialize_database` (pattern of the existing ledger at lines 284–331) that rewrites every connection payload lacking `wire == "a2a"` to disconnected/`superseded_wire_contract`/credential `None`, drops `inbound_secret`, stamps `connection_disconnected_at` on its dispatched non-terminal runs, counts the rows on the ledger row and writes one `wire_superseded` audit row per connection — with the wire-less-row rewrite itself idempotent and run on every startup outside the ledger, as `_migrate_legacy_invalid_connections` already does — the conditional run `UPDATE … WHERE owner_id=? AND id=? AND run_version=?`, the due-observation query, and `export_owner_data` exclusions (014-FR-012, 014-SC-010, 014-FR-008, 014-SC-007)
- [x] T031 Add the rollback-compatible document shape to `backend/app/modules/agents/domain.py` to make T022 pass: `AgentConnectionDocument.wire: Literal["a2a"]` and `disconnect_reason`, `schema_version` 2 on rewritten documents, and the manifest's inert `reporting`/`reporting_instructions`/`instructions_version` placeholders with `AgentReportingContract` reduced to a frozen placeholder that only reproduces the 007 shape (never read by 014, never exposed by the API) (014-FR-012, 014-SC-010)
- [x] T032 Add the constructor ports without behaviour change: `AgentRelayService` in `backend/app/modules/agents/service.py` accepts an `A2AClientPort` and an `exchange_executor` (a `Callable`/executor protocol) beside the existing `ConnectorPort`, and `backend/app/container.py` constructs `A2AClient` from `AgentRelaySettings` and passes it in — the observer and the pools arrive in T066/T070, the bespoke connector leaves in T110/T115, so no construction cycle ever exists; nothing changes behaviour here, so the parity guardrail is the T001 green baseline (`cd backend && pytest -q`), which must stay green after this task (014-FR-004, 014-FR-007)
- [x] T033 [P] Publish the extension specification at `docs/a2a-extensions/single-start/v1.md` from `contracts/brainbuddy-single-start-extension.md` (identifier, declaration, activation, the entire §4 contract, the informative client behaviour, the §6 conformance test, security and versioning; the path is frozen by repository policy and never moves) so the file case of T017 passes (014-FR-011, 014-FR-003)

**Checkpoint**: Foundation ready — the protocol surface, deadline, limiter, redaction, columns, migration and ports exist; user story implementation can now begin, with the frontend and mobile lanes parallel to the backend lane once each story's schema task has landed.

---

## Phase 3: User Story 1 - Connect an A2A agent by its card (Priority: P1) 🎯 MVP

**Goal**: A signed-in owner adds an agent by address plus one of exactly two credential schemes; BrainBuddy discovers it through its published card, tests it with an authenticated `ListTasks`, shows the discovery result and the guarantee tier, names every failure category (including **rate limited** and **Agent changed**), and renders every card-sourced string inertly on web and iOS (plan.md §1; FR-001, FR-002, FR-003, FR-004, FR-011, FR-016; AC-001..AC-006, AC-031, AC-037).

**Independent Test**: Add the official helloworld sample and the Hermes stub as connections (quickstart.md §1 step 1, §2 step 1), complete recent reauthentication, test each, rotate a credential to a wrong value and test again, and inspect the saved connection without dispatching a task — D-01-S10/S11/S12 and M-01-S09/S10 on the two clients.

### Tests for User Story 1 ⚠️

> **NOTE: Write these tests FIRST, ensure they FAIL before implementation**

- [x] T034 [US1] Write the failing connection cases in `backend/tests/test_agent_relay_service.py` (names carry `014_FR_001`, `014_FR_002`, `014_FR_003`, `014_FR_004`, `014_FR_014`, `014_SC_003`, `014_SC_009`; AC-001..AC-006, AC-037 in docstrings): `test_connection` returns **ready** only after an authenticated `ListTasks(pageSize=1)` (with the `-32601` fallback `GetTask {id:"brainbuddy-probe"}` → `-32001`), **invalid credentials** on 401/403/`-32050`/`-32052` with the secret never echoed, **unreachable** on transport failure, test timeout or `EgressDeadlineExceeded`, the four **unsupported** sentences with their `last_test_error_detail` shapes, **private destination** refused before any credential leaves, `test_rate_limited_connection_test_keeps_status_untested_and_records_retry_after` (429/`-32051`, `status` stays `untested`, retry after `retry_after_seconds`); tier `guaranteed` iff the card declares the FR-011 URI with the server-owned `tier_disclosure`, `tier_disclosure_url` and `cancellation_disclosure`, and `test_014_FR_014_tier_disclosures_carry_the_exact_server_owned_copy` asserting that `tier_disclosure` begins with exactly `Guaranteed single start.` or `Best-effort single start.` (the FR-014 literals are server-owned, so the exact copy is asserted once, here); card drift (`agent_card_changed`) puts the connection back to `untested`, sets `card_drift_at`, resets `scope_verified_at` and clears `best_effort_acknowledged_at`, as do an address change, an auth-scheme change and a credential rotation; `auth_header_name` is copied from the card only, absent for bearer, refused when reserved; the card summary and `last_contact_at` refresh on every successful test and the 007 stale rule holds; SC-003 re-targeting — cross-owner 404, malformed and oversized card bodies through `httpx.MockTransport`, DNS rebinding through `fake_resolver`, stale connection refused — each with zero state change and zero secret disclosure (014-FR-001, 014-FR-002, 014-FR-003, 014-FR-004, 014-FR-014, 014-SC-003, 014-SC-009)
- [x] T035 [US1] Write the failing connection-route cases in `backend/tests/test_agent_relay_api.py` (`014_FR_001`, `014_FR_002`, `014_FR_016`, `014_SC_009`; AC-031 in docstrings): `POST /agent-connections` accepts `auth_scheme` and rejects `auth_header_name`, returns 201 with a plain `AgentConnectionResponse` and no secret; `test_card_text_is_returned_verbatim_and_bounded` (a card whose description carries markup and whose `interface_url` is `javascript:…` or `http://169.254.169.254/latest/meta-data/` comes back verbatim within the data-model.md §1 bounds); `POST …/test` answers `last_test_error_code: a2a_rate_limited` with `status: untested` and `last_test_error_detail.retry_after_seconds`; `agent_changed`, `disconnect_reason`, `correlation_id_honoured`, `tier_disclosure_url` and the closed `last_test_error_detail` shapes on every read; reasons `a2a_auth_scheme_unsupported`, `agent_card_changed`, `a2a_rate_limited` on the error envelope with `X-Correlation-ID`; the rollout-OFF matrix for create/update/test/credential kept (014-FR-001, 014-FR-002, 014-FR-016, 014-SC-009)
- [x] T036 [US1] Write the failing connection-schema cases in `backend/tests/test_agent_schemas.py` (`014_FR_001`, `014_FR_002`, `014_FR_003`): `AgentConnectionCreateRequest`/`UpdateRequest` with `auth_scheme` and `agent_address`, `AgentConnectionResponse` per `contracts/api-deltas.md` (`card`, `guarantee_tier`, `tier_disclosure`, `tier_disclosure_url`, `capabilities` as `{streaming, push_notifications}` and `controls_offered` as `{reply, cancel}` — BrainBuddy-side, never an agent claim —, `agent_changed`, `best_effort_acknowledged_at`, `disconnect_reason`, `correlation_id_honoured`, `last_test_error_detail`), and no protocol field name in any client-facing schema (014-FR-001, 014-FR-002, 014-FR-003, 014-FR-014)
- [x] T037 [US1] Write the failing connection cases in `frontend/src/api/__tests__/agentClient.test.ts` and `frontend/src/api/__tests__/agentHooks.test.tsx` (`014-FR-001`, `014-FR-002`): create/update send `agent_address` and `auth_scheme`, never `auth_header_name`; the test mutation surfaces `last_test_error_code`, `last_test_error_detail`, `card`, `guarantee_tier`, `tier_disclosure_url` and `agent_changed`; connection queries invalidate after test, rotate and update (014-FR-001, 014-FR-002)
- [x] T038 [US1] Write the failing per-state suite in `frontend/src/features/agents/__tests__/AgentSettingsPage.test.tsx` for D-01-S01..S22, S24 and S25 (`014-FR-001`, `014-FR-002`, `014-FR-003`, `014-FR-014`, `014-FR-016`; AC-031, AC-037 in the test names' stories): the add form with the scheme radio and the read-only card-sourced header name (S08/S09), the discovery result with tier and the extension link that opens only on explicit click with an external marker (S10/S11), every failure category as its own sentence with a correlation ID (S12–S18), stale (S19), **Agent changed** with the tested and current interface side by side (S20), **Superseded wire contract** with no reuse path (S21), rollout OFF disabling add/edit/test/rotate through real disabled `<button>`s while disconnect stays (S22), rotate never showing the current value (S24), **rate limited** with "Test again in about N seconds." and the CHK051 absence copy "Test again shortly." with **Test connection** still offered (S25); the AC-031 cases render a markup-bearing description and a `javascript:`/metadata `interface_url` as plain text with no anchor and no navigable element; offline (S07) disables every secret-bearing action; focus trap, Escape and focus restore on the add form and rotate dialog (D-01-S23 belongs to T102) (014-FR-001, 014-FR-002, 014-FR-003, 014-FR-014, 014-FR-016)
- [x] T039 [P] [US1] Write the failing mobile suites `mobile/src/features/agents/__tests__/AddConnectionSheet.test.tsx` (M-01-S08: address, scheme radios, credential, password, "Read from the agent card when you test", silent discard on dismiss) and `mobile/src/features/agents/__tests__/ConnectionCard.test.tsx` (M-01-S01, S09..S20, S22; `014-FR-001`, `014-FR-002`, `014-FR-003`, `014-FR-016`): the discovery result as a two-column list with skill pills and tier, every category sentence, **Agent changed**, **Superseded wire contract**, rollout OFF stated in copy with disconnect still offered, **rate limited** with the retry-after hint or the CHK051 "Test again shortly." copy and a 44pt **Test connection**; the AC-031 cases assert no `Linking` target and no auto-link for the same adversarial card values; disabled actions carry the accessibility disabled state (M-01-S07) (014-FR-001, 014-FR-002, 014-FR-003, 014-FR-016)
- [x] T040 [US1] Write the failing mobile contract cases in `mobile/src/api/__tests__/agentClient.test.ts`, `mobile/src/api/__tests__/hooks.test.tsx`, `mobile/src/features/agents/__tests__/connectionMutationIdempotency.test.tsx` and the screen suite `mobile/src/app/__tests__/agents.test.tsx` against `mobile/src/test/fakeBackend.ts` (`014-FR-001`, `014-FR-002`), updating `mobile/src/test/agentFixtures.ts` and `mobile/src/test/fakeBackend.ts` to the 014 connection shape: create/update carry `auth_scheme` in the canonical identity, create returns no secret and the screen shows no secret reveal, test/rotate/update invalidate the connection list (014-FR-001, 014-FR-002)
- [x] T041 [P] [US1] Extend the real-backend harness `mobile/integration/run.ts` (and the disposable backend's environment in `mobile/integration/backend.ts` with `BRAIN_BUDDY_AGENT_ALLOW_PRIVATE_DESTINATIONS=1`) with the connection-test contract (`014-FR-002`): create a connection, test it against the loopback fake agent of `backend/tests/a2a_fakes.py` started as a subprocess by the harness, and assert `ready`, `a2a_credentials_rejected`, `a2a_unreachable` and `a2a_not_an_agent` come back through the mobile client with no secret in any response (014-FR-002, 014-SC-009)

### Implementation for User Story 1

- [x] T042 [US1] Add the connection fields to `backend/app/modules/agents/domain.py` to make the domain cases of T034 pass: `auth_scheme`, the `auth_header_name` rules (stored only for `api_key`, never reserved, key absent otherwise), `card: AgentCardSummary | None`, `card_fingerprint`, `card_drift_at`, `guarantee_tier`, `best_effort_acknowledged_at`, `context_id_honoured`, `last_test_error_code` vocabulary, `last_test_error_detail` (≤ 512 bytes, closed per-code shapes), the derived `agent_changed`, and the connection state transitions of data-model.md §1; the `endpoint_url` storage key stays (014-FR-001, 014-FR-002, 014-FR-003)
- [x] T043 [US1] Rewrite `backend/app/modules/agents/headers.py` in one ordered pass (this is the only pass on this file): the reserved list keeps every transport-structural name it has today (`Host`, `Content-Length`, `Transfer-Encoding`, `Connection`, `Proxy-Authorization`, `Proxy-Authenticate`, `TE`, `Trailer`, `Upgrade`, `Keep-Alive`, `Accept`, `Content-Type`, `User-Agent`, `Authorization`), drops only `Idempotency-Key` and `X-BrainBuddy-Run-Id`, adds `A2A-Version`, `A2A-Extensions` and `X-Correlation-ID`, and validates card-sourced header names as HTTP field-name tokens (014-FR-001, 014-FR-004, 014-FR-012)
- [x] T044 [US1] Implement discovery, test, tier and drift in `backend/app/modules/agents/service.py` to make T034 pass: `create_connection`/`update_connection`/`rotate_credential` with `auth_scheme` and the scope reset that also clears `best_effort_acknowledged_at`; `test_connection` through `a2a/card.py` plus an authenticated `ListTasks(pageSize=1)` on the `A2AClientPort` with the six categories, the rate-limited outcome and the closed error details; tier detection with the server-owned `tier_disclosure`, `tier_disclosure_url` and `cancellation_disclosure`; `card_fingerprint` drift detection on test, preview and dispatch (`agent_card_changed`, `card_drift_at`, `scope_verified_at` reset); `_target` derived from the card interface; audit `card_fetched`, `connection_tested`, `card_drift_detected`; `capabilities` becomes `{streaming, push_notifications}` (the card's declarations only) beside `controls_offered` `{reply: true, cancel: true}` (BrainBuddy-side, never an agent claim); the bespoke probe path stays dead until T110 removes it (014-FR-001, 014-FR-002, 014-FR-003, 014-FR-004, 014-FR-011, 014-SC-009)
- [x] T045 [US1] Change the connection contract first in `backend/app/schemas/agents.py` to make T036 and the served-URL case of T017 pass: request schemas with `auth_scheme` required and `auth_header_name` rejected, `AgentConnectionResponse` per `contracts/api-deltas.md`, create returning `AgentConnectionResponse` (201), and the new `ValidationFailure.detail.reason` values `a2a_auth_scheme_unsupported` (the same code as `last_test_error_code`), `agent_card_changed`, `a2a_rate_limited` (014-FR-001, 014-FR-002, 014-FR-003, 014-FR-011)
- [x] T046 [US1] Update the connection routes in `backend/app/api/agents.py` to make T035 pass: create answers 201 with the plain response, update requires `current_password` when `agent_address` or `auth_scheme` changes, test/credential/disconnect keep their request shapes, the `external_agent_relay` gate stays, and each handler keeps computing `reauthenticated = _verify_password(auth_service, current_user, current_password)` and passing `reauthenticated` into the service, which decides through `requires_dispatch_reauthentication` (that per-handler `_verify_password` call is the whole route-side mechanism; the routes have no reauthentication helper of their own), services arrive via `Depends()` (014-FR-001, 014-FR-004, 014-FR-016)
- [x] T047 [US1] Mirror the connection contract in `frontend/src/api/agentTypes.ts`, `frontend/src/api/client.ts` and `frontend/src/api/agentHooks.ts` to make T037 pass: `agent_address`, `auth_scheme`, `card`, `guarantee_tier`, `tier_disclosure`, `tier_disclosure_url`, `agent_changed`, `disconnect_reason`, `correlation_id_honoured`, `last_test_error_detail`, the new `last_test_error_code` values and the `capabilities` / `controls_offered` shapes (014-FR-001, 014-FR-003)
- [x] T048 [US1] Realise D-01-S01..S22, S24 and S25 in `frontend/src/features/agents/AgentSettingsPage.tsx` with the sentences in `frontend/src/features/agents/agentCopy.ts` to make T038 pass: the add form with the scheme radio and read-only header name (S08/S09), discovery result, tier and extension link (S10/S11), the category sentences (S12–S18), stale (S19), **Agent changed** (S20), **Superseded wire contract** (S21), rollout OFF (S22), rotate (S24), **rate limited** with "Test again in about N seconds." or, when `retry_after_seconds` is null, "Test again shortly." (S25, CHK051); every card-sourced value and error detail rendered as inert plain text; real `<button>` controls with `disabled`, focus containment, Escape and focus restore; `SigningSecretPanel` and `ReplaceSigningSecretDialog` have no D-01 state and are removed here (their types leave in T116) (014-FR-001, 014-FR-002, 014-FR-003, 014-FR-014, 014-FR-016)
- [x] T049 [US1] Mirror the connection contract in `mobile/src/api/types.ts`, `mobile/src/api/client.ts` and `mobile/src/api/hooks.ts` to make T040 pass (the same fields as T047; create returns no secret) (014-FR-001, 014-FR-003)
- [x] T050 [P] [US1] Realise M-01-S08 in `mobile/src/features/agents/AddConnectionSheet.tsx` and M-01-S01, S09..S20, S22 in `mobile/src/features/agents/ConnectionCard.tsx` to make T039 pass, and drop the post-create secret reveal and the secret sheets' mounting from `mobile/src/app/agents/index.tsx` (the sheet files are deleted in T116): `Pressable` controls with the disabled accessibility state, 44pt rows wrapping two-up, card-sourced text rendered inertly and never a `Linking` target, the extension link on explicit tap with an external marker, the CHK051 copy on M-01-S22 (014-FR-001, 014-FR-002, 014-FR-003, 014-FR-014, 014-FR-016)

**Checkpoint**: User Story 1 is functional and testable on its own — connections can be created, tested and inspected against both vendored runtimes (quickstart.md §1 step 1, §2 step 1) with no dispatch.

---

## Phase 4: User Story 2 - Review and hand one task to the agent (Priority: P1)

**Goal**: The consent boundary: the immutable manifest (now with message ID, correlation ID, both disclosures, the one-time best-effort acknowledgement and the push-callback disclosure), then one dispatch whose exchange is held in the background under an absolute deadline, honest **Queued / Sent / Delivery unconfirmed / Not sent** states, lookup-before-resend with its preconditions, restart recovery that never resends, and barrier-safe **Check again** (plan.md §2; FR-003, FR-004, FR-005, FR-006, FR-007; AC-007..AC-012, AC-026, AC-027, AC-030, AC-032, AC-034, AC-036; SC-002, SC-008).

**Independent Test**: Open one Task, select a tested connection, remove one supporting item, tick the one-time acknowledgement on a best-effort connection, confirm, replay the confirmation three times and verify exactly one task exists at the agent (`ListTasks` by correlation ID, quickstart.md §1 steps 2–3, §3); then force an ambiguous dispatch and recover it with **Check again** (D-03-S05 / M-03-S04).

### Tests for User Story 2 ⚠️

> **NOTE: Write these tests FIRST, ensure they FAIL before implementation**

- [x] T051 [US2] Write the failing manifest and acknowledgement cases in `backend/tests/test_agent_relay_service.py` (`014_FR_003`, `014_FR_005`, `014_FR_014`, `014_SC_005`; AC-007, AC-008, AC-011, AC-012, AC-026 in docstrings): the manifest lists every value that leaves — title, details when selected, each supporting item, Task ID, run ID, `message_id`, `correlation_id`, destination interface, tier disclosure with `tier_disclosure_url`, cancellation disclosure, external-copy notice, and `push_callback` (`registered`, token-masked `url_preview`, the server-owned disclosure sentence) only when the card supports push, with the token in no response; the 007 token discipline extended so a change of value, destination, callback origin or run ID invalidates confirmation; preview refetches the card and refuses drift with `agent_card_changed`; stale, untested, disconnected and wrong-owner connections are refused before any content; cancelling creates no run; `test_first_best_effort_confirm_without_acknowledgement_is_refused` (`duplicate_risk_acknowledgement_required` before any reservation), `test_acknowledgement_flag_is_ignored_once_persisted`, `test_best_effort_acknowledged_at_is_persisted_and_reset_with_scope` (address change, auth-scheme change, credential rotation, card drift) `acknowledgement_required: false` for the guaranteed tier, and `test_014_FR_014_cancellation_disclosure_carries_the_exact_server_owned_copy` asserting that the manifest's `cancellation_disclosure` begins with exactly `Cancellation depends on the agent` (the third FR-014 literal, asserted once here beside the two tier literals of T034) (014-FR-003, 014-FR-005, 014-FR-014, 014-SC-005)
- [x] T052 [US2] Write the failing dispatch and exchange cases in `backend/tests/test_agent_relay_service.py` (`014_FR_006`, `014_FR_007`, `014_SC_002`; AC-009, AC-010, AC-034 in docstrings): the durable reservation before I/O (`context_id = run.id`, `message_id = f"{run.id}:start"`, Task and run ids as metadata, inline `taskPushNotificationConfig` when the card supports push, `A2A-Extensions` plus `message.extensions` on the first send and every replay for a guaranteed-tier agent); `exchange_state` `queued` at reservation and `open` with `exchange_started_at`/`exchange_deadline_at` stamped when the worker starts; dispatch waits `dispatch_wait_seconds`, probes `ListTasks(contextId, pageSize=5)` only when the exchange is open and returns the **Queued** projection with no probe while it is still queued; outcomes — Task or Message answer → `sent` (adopted only when `task.contextId == run.id`; a foreign `contextId` is `a2a_response_invalid` and records `context_id_honoured=false`), a direct Message → completed, a terminal state inside the exchange → **Sent** then the terminal state, `ConnectError`/401/403/`-32602` → `not_sent`, 429 → `not_sent` with `a2a_rate_limited` retryable with the same ids, timeout/5xx/`EgressDeadlineExceeded` → `delivery_unconfirmed`; `test_014_SC_002_three_replays_create_one_task` against the helloworld-shaped fake, the Hermes-shaped fake and the extension fake whose `ListTasks` is empty (the identical resend is deduped by message ID); a replay with the same `Idempotency-Key` while the exchange is open returns the live projection without a second exchange; `context_id_honoured=false` never resends, not even on **Check again**; `test_purge_during_open_exchange_writes_no_row` and `test_disconnect_during_open_exchange_does_not_overwrite` (014-FR-006, 014-FR-007, 014-SC-002)
- [x] T053 [US2] Write the failing check-delivery, retry-reauthentication and barrier cases in `backend/tests/test_agent_relay_service.py` (`014_FR_004`, `014_FR_006`, `014_SC_008`; AC-027, AC-030, AC-032, AC-036 in docstrings): `check_delivery` looks the run up once by correlation ID, adopts a found task → `sent`, resends the identical message once on an empty answer and never mints a new run or message ID; `test_check_delivery_refuses_resend_when_connection_not_ready`, `…_when_agent_card_changed` (connection **Agent changed** or the run's pinned fingerprint differs), `…_when_scope_reset_since_dispatch`, `…_when_run_content_expired`, each asserting zero outbound `SendMessage` and the run left `delivery_unconfirmed`, and `…_when_connection_disconnected`, `…_when_run_terminal`, `…_when_agent_task_missing` refusing the whole check before any lookup (the eight reasons of FR-006); the reauthentication cases follow the `first_dispatch_at` rule of FR-004 — `test_retry_of_a_hand_off_restarted_before_send_requires_dispatch_reauthentication` is the failing case: a confirmed hand-off is `queued`, the connection's `first_dispatch_at` stays `None` because nothing left BrainBuddy, a restart settles the run as `not_sent`/`restarted_before_send`, the `Clock` advances past the 15-minute window, and the retry with the same run ID, message ID and frozen manifest token is refused with `reauthentication_required` and zero outbound `SendMessage` until `current_password` is supplied, after which the exchange starts and `first_dispatch_at` is stamped; `test_resend_of_a_started_exchange_lies_inside_the_spent_reauthentication_trigger` (an exchange that started stamped `first_dispatch_at`, so **Check again** on its `delivery_unconfirmed` run needs no password) and `test_check_delivery_lookup_needs_no_reauthentication`; the resend goes to the run's pinned `interface_url` after re-checking `card_fingerprint`; on the `BlockingA2AAgent` port the start barrier re-targeted from `test_concurrent_same_key_dispatches_contend_and_converge_only_with_lock`, plus `test_concurrent_check_delivery_requests_converge_on_one_resend`, `test_check_delivery_racing_a_replayed_confirmation_resends_once` and `test_check_delivery_racing_restart_recovery_resends_once` — one durable `open` winner through the compare-and-set on `run_version` under the command lock, the loser returning without network I/O; `test_rollout_off_check_delivery_looks_up_then_refuses_with_rollout_disabled` (the lookup runs and a found task is adopted; an empty lookup is refused with `rollout_disabled` after it ran, zero `SendMessage`) (014-FR-004, 014-FR-006, 014-SC-008)
- [x] T054 [US2] Write the failing exchange-side observer cases in `backend/tests/test_agent_observer.py` with `AgentObserver(exchange_executor=SynchronousExecutor(), observation_executor=SynchronousExecutor(), control_executor=SynchronousExecutor(), clock=Clock, probe_delay=0)` and no sleeps (`014_FR_006`, `014_FR_007`, `014_FR_008`; AC-032, AC-034 in docstrings): `test_restart_recovery_settles_a_queued_exchange_as_not_sent_restarted_before_send` (never **Delivery unconfirmed**, hand-off re-offered with the same ids), `test_restart_recovery_resolves_an_open_exchange_by_lookup_only` (adopt or stay `delivery_unconfirmed`, zero `SendMessage`), `test_one_connection_holds_at_most_max_exchanges_per_connection` (excess exchanges stay `queued` while another connection's proceed), owner-round-robin service of queued exchanges, the exchange half of `test_drip_feeding_agent_releases_the_worker_at_the_deadline` (→ `delivery_unconfirmed`), stamping at start not submit (`exchange_started_at`, `exchange_deadline_at` and, in the same write, the connection's `first_dispatch_at` when still unset — never at reservation, so a queued exchange leaves it `None`), `test_restart_recovery_is_invoked_once_at_boot_under_background_maintenance` (the `main.py` startup hook calls `recover_interrupted_exchanges()` exactly once under `background_maintenance_enabled` and not at all under TEST without the opt-in — the guardrail T070 cites), and `shutdown(wait=False, cancel_futures=True)` cancelling only unstarted futures (014-FR-006, 014-FR-007, 014-FR-008)
- [x] T055 [US2] Write the failing hand-off route cases in `backend/tests/test_agent_relay_api.py` and the route-matrix additions in `backend/tests/test_api_contract.py` (`014_FR_005`, `014_FR_006`, `014_FR_016`, `014_SC_009`; AC-026, AC-032, AC-036 in docstrings): the `AgentManifestResponse` shape of `contracts/api-deltas.md` with `acknowledgement_required`, `push_callback` and `parts_preview`; preview drift → 400 `agent_card_changed`; dispatch with `acknowledge_duplicate_risk` in the request identity and 400 `duplicate_risk_acknowledgement_required` before any reservation; `primary_state_label: "Queued"` while queued and `Not sent` with `dispatch_error_code: restarted_before_send`; `POST /agent-runs/{id}/check-delivery` with `Idempotency-Key` and optional `current_password`, its complete eight 400 reasons — `connection_not_ready`, `agent_card_changed`, `run_content_expired`, `reauthentication_required`, `rollout_disabled`, `run_terminal`, `agent_task_missing`, `connection_disconnected` (the same list as FR-006, `contracts/api-deltas.md` and T068) — and the unchanged run when already `sent`; `test_rollout_off_check_delivery_looks_up_but_never_resends` (the lookup runs and adopts a found task; an empty lookup answers 400 `rollout_disabled` after it ran, zero `SendMessage`) and rollout OFF for preview/dispatch; every error carries `X-Correlation-ID` (014-FR-005, 014-FR-006, 014-FR-016, 014-SC-009)
- [x] T056 [US2] Write the failing hand-off schema cases in `backend/tests/test_agent_schemas.py` (`014_FR_005`, `014_FR_006`, `014_FR_014`): `AgentManifestResponse` deltas (`supporting_items`, `message_id`, `correlation_id`, `destination_interface`, `protocol_version "1.0"`, `guarantee_tier`, `tier_disclosure`, `tier_disclosure_url`, `acknowledgement_required`, `cancellation_disclosure`, `push_callback`), the dispatch request with `acknowledge_duplicate_risk`, the check-delivery request, and the dispatch-side `AgentRunResponse` fields (`run_version`, `message_id`, `correlation_id`, `guarantee_tier`, `exchange_open`, `exchange_state`, `exchange_kind`, `dispatch_error_code` values, `push_registration`) with no protocol field name anywhere (014-FR-005, 014-FR-006, 014-FR-014)
- [x] T057 [US2] Write the failing push-token case in `backend/tests/test_agent_secrets.py` (`014_FR_008`): a 32-byte urlsafe per-run token is stored only as the keyed `SecretBox.fingerprint`, compared in constant time, and never persisted or logged in plaintext (014-FR-008)
- [x] T058 [P] [US2] Write the failing per-state suite in `frontend/src/features/agents/__tests__/AgentHandoffOverlay.test.tsx` for D-02-S01..S15 with the SC-005 review cases named `014-SC-005` (`014-FR-003`, `014-FR-005`, `014-FR-014`; AC-007, AC-008, AC-026 in the stories): every manifest value listed at desktop width, both disclosures, the masked push callback address and its disclosure only when `push_callback.registered`, each supporting item removable through `Remove <item>` with the manifest rebuilt, the details toggle, Cancel creating nothing, the best-effort block with the extension link opening only on explicit click with an external marker (S02), loading with "no task content has been sent" (S03), the two empty states listing **Agent changed** among ineligible reasons (S04/S05), error with correlation ID (S06), "Status unknown" not selectable (S07), re-review (S08), refused agent changed (S09), offline send disabled (S10), reauthentication (S11), sending disabled and not dismissible (S12), the acknowledgement unticked disabling **Send to agent** (S13), ticked enabling it (S14), later hand-off without the control (S15); the AC-031 case: an agent name and destination interface carrying markup, a `javascript:` address or a metadata address render as plain text with no anchor, no navigable element and no auto-link; focus containment with the close control first and **Send to agent** last, Escape on non-destructive states, focus restored to **Hand to agent** (014-FR-003, 014-FR-005, 014-FR-014, 014-FR-016, 014-SC-005)
- [x] T059 [US2] Write the failing dispatch-state cases in `frontend/src/features/agents/__tests__/AgentRunSection.test.tsx` and `frontend/src/features/agents/__tests__/agentCopy.test.ts` (the dispatch part of the `014-SC-004` suite; `014-FR-006`, `014-FR-014`; AC-027, AC-032, AC-034 in the stories): D-03-S03 **Not sent** for the rate-limited category and the restart variant "BrainBuddy restarted before this hand-off was sent. Nothing left BrainBuddy." with **Try this hand-off again** reopening the review seeded from the run's frozen manifest — confirming it unchanged posts the same request identity (`connection_id`, `include_details`, `supporting_items`, the frozen `manifest_token`, `acknowledge_duplicate_risk`) under the `agent-handoff-<token>` `Idempotency-Key` the overlay already derives, so the response carries the same run ID and message ID; a changed review previews again and the response carries a new run ID while the old run stays **Not sent**; and after a restart the password field is shown again when the server answers `reauthentication_required` (FR-004) —, D-03-S04 **Sent** and its **Queued** variant "Waiting for a free connection slot; nothing has been sent yet" whenever `exchange_state == "queued"`, D-03-S05 **Delivery unconfirmed** with **Check again** posting `check-delivery` with the run's own ids and never a new run (014-FR-006, 014-FR-014, 014-SC-004)
- [x] T060 [US2] Write the failing hand-off client cases in `frontend/src/api/__tests__/agentClient.test.ts` and `frontend/src/api/__tests__/agentHooks.test.tsx` (`014-FR-005`, `014-FR-006`): preview with `supporting_items`, dispatch with `acknowledge_duplicate_risk` inside the canonical request identity and the manifest token, `checkAgentRunDelivery` with `Idempotency-Key` and optional `current_password`, run invalidation after each (014-FR-005, 014-FR-006)
- [x] T061 [P] [US2] Write the failing mobile review suites `mobile/src/features/agents/__tests__/HandoffSheet.test.tsx`, `mobile/src/features/agents/__tests__/HandoffSheet.lifecycle.test.tsx` and `mobile/src/features/agents/__tests__/agentIdempotency.test.tsx` for M-02-S01..S14 with the SC-005 cases named `014-SC-005` (`014-FR-003`, `014-FR-005`, `014-FR-014`; AC-007, AC-010, AC-026 in the stories): fixed header, scrolling body, the pinned footer with **Send to agent** and **Cancel** present at every scroll position, every **Remove** control named for its item, the masked push callback and its disclosure, the 44pt acknowledgement row disabling Send with the accessibility disabled state (S12), ticked (S13), later hand-off without it (S14), offline and app closure rebuilding from the server (S10), not swipe-dismissible in flight (S11), a replayed confirmation returning the same run with `acknowledge_duplicate_risk` in the identity, and the AC-031 case: the same adversarial agent name and destination interface rendered with no `Linking` target and no auto-link (014-FR-003, 014-FR-005, 014-FR-014, 014-FR-016, 014-SC-005)
- [x] T062 [US2] Write the failing mobile dispatch-state cases in `mobile/src/features/agents/__tests__/TaskAgentSection.test.tsx`, `mobile/src/features/agents/__tests__/AgentRunSection.test.tsx`, `mobile/src/agents/__tests__/machine.test.ts`, `mobile/src/api/__tests__/agentClient.test.ts` and `mobile/src/api/__tests__/hooks.test.tsx` (the dispatch part of `014-SC-004`; `014-FR-006`, `014-FR-014`): M-03-S02 both variants with the 44pt **Try this hand-off again**, M-03-S03 with its **Queued** variant, M-03-S04 with the 44pt **Check again** row disabled offline, machine transitions for `exchange_state` and `restarted_before_send`, and the check-delivery call with `Idempotency-Key` (014-FR-006, 014-FR-014, 014-SC-004)

### Implementation for User Story 2

- [x] T063 [US2] Add the push-token helper to `backend/app/modules/agents/secrets.py` (generate 32 urlsafe random bytes, keyed fingerprint, constant-time comparison) to make T057 pass; the inbound-signing purposes are removed in T113 (014-FR-008)
- [x] T064 [US2] Add the run and manifest shape to `backend/app/modules/agents/domain.py` to make the domain cases of T051–T053 pass: the run fields of data-model.md §2 (`context_id`, `message_id`, `agent_task_id`, `interface_url`, `card_fingerprint`, `guarantee_tier`, `run_version`, `exchange_state`, `exchange_kind`, `exchange_started_at`, `exchange_deadline_at`, `push_registration`, `push_token_fingerprint`, `next_observation_at`, `last_observed_at`, `observation_trigger_pending`, `agent_task_missing_at`, `cancel_outcome`, `blocked_reason`, `artifacts_summary`, `result_availability`, `identifiers_expire_at`, `identifiers_expired`), the manifest additions (`message_id`, `correlation_id`, tier and cancellation disclosures, `acknowledgement_required`, `push_callback`, `protocol_version "1.0"`), the dispatch state machine, and `_primary_state_label` with **Queued** checked before the dispatch labels (014-FR-005, 014-FR-006, 014-FR-008, 014-FR-014)
- [x] T065 [US2] Add the dispatch persistence to `backend/app/modules/agents/repository.py` to make the repository cases of T052–T054 pass: reservation writes with the exchange columns, the `queued → open → closed | interrupted` transitions — the `queued → open` write stamps `exchange_started_at`, `exchange_deadline_at` and the connection's `first_dispatch_at` when it is still unset, so the reauthentication trigger is spent by the exchange start and never by the reservation —, the startup queries for queued and open exchanges, the compare-and-set `interrupted|delivery_unconfirmed → open` under the process-wide `command_lock`, the `best_effort_acknowledged_at` stamp under the same lock, the dispatch receipt carrying `exchange_state`, the `context_id_honoured` write, and the audit actions `run_dispatched`, `exchange_closed`, `task_adopted` (014-FR-003, 014-FR-006, 014-SC-008)
- [x] T066 [US2] Create `backend/app/modules/agents/observer.py` (exchange responsibilities) to make T054 pass: `AgentObserver` owning the exchange pool (`agent_relay.exchange_workers`), the per-connection bound (`max_exchanges_per_connection`, excess stays `queued`), owner-round-robin submission, the worker starting the exchange through the T065 `queued → open` transition (which stamps `exchange_started_at`/`exchange_deadline_at` and the connection's `first_dispatch_at` at start, never at submit), every exchange outcome handed to `AgentRelayService.apply_observation`, `recover_interrupted_exchanges()` settling a queued exchange as `not_sent`/`restarted_before_send` and flipping an open start exchange to `interrupted` for lookup-only resolution, injectable executors, `Clock` and `probe_delay`, and `shutdown(wait=False, cancel_futures=True)`; scheduling, the observation and control pools and the wake port follow in T087 (014-FR-006, 014-FR-007, 014-FR-008)
- [x] T067 [US2] Implement the hand-off path in `backend/app/modules/agents/service.py` to make T051–T053 pass: `_build_manifest` deltas with the push-callback disclosure and `acknowledgement_required`; preview refetch and drift; the AC-026 acknowledgement gate; `dispatch_run` with the exchange shape (reserve without stamping `first_dispatch_at` — the 007 stamp before any I/O in `dispatch_run` moves to the exchange start of T065, so a queued hand-off that never left is still a first dispatch under `requires_dispatch_reauthentication` when retried —, mark `queued`, submit to the exchange executor, wait `dispatch_wait_seconds`, inline probe only when open, the **Queued** projection, the outcome table, adoption safety, `context_id_honoured`, inline push config, `A2A-Extensions` for the guaranteed tier); a first `apply_observation(run_id, owner_id, observation, based_on)` that re-reads under `command_lock`, compares-and-sets `run_version`, locks terminal states and appends the timeline row with `trigger=dispatch` with no I/O inside the lock (extended in T088); `check_delivery` with the resend preconditions (the eight reasons of FR-006, `rollout_disabled` returned after the lookup ran), `requires_dispatch_reauthentication`, the pinned `interface_url`, the fingerprint re-check, the compare-and-set serialisation and the rollout-OFF lookup-only rule; the replayed-confirmation path sharing it; audit rows with server-minted correlation ids (014-FR-003, 014-FR-004, 014-FR-005, 014-FR-006, 014-FR-007, 014-SC-002, 014-SC-008)
- [x] T068 [US2] Change the hand-off contract in `backend/app/schemas/agents.py` to make T056 pass: `AgentManifestResponse`, the dispatch request with `acknowledge_duplicate_risk`, the check-delivery request, the dispatch-side `AgentRunResponse` fields, the added `ValidationFailure.detail.reason` values (`duplicate_risk_acknowledgement_required`, `rollout_disabled`, and on check-delivery the complete eight — `connection_not_ready`, `agent_card_changed`, `run_content_expired`, `reauthentication_required`, `rollout_disabled`, `run_terminal`, `agent_task_missing`, `connection_disconnected` — the same list as FR-006, `contracts/api-deltas.md` and T055), and the `supporting_items`/`correlation_id` vocabulary (014-FR-005, 014-FR-006, 014-FR-014)
- [x] T069 [US2] Add `POST /agent-runs/{id}/check-delivery` (`Idempotency-Key`, service via `Depends()`; the lookup allowed while rollout is OFF and the resend refused inside the service) and the `acknowledge_duplicate_risk` dispatch field to `backend/app/api/agents.py` to make T055 pass; preview and dispatch keep their gate and reauthentication (014-FR-006, 014-FR-016)
- [x] T070 [US2] Wire the exchange lane: `backend/app/container.py` creates the exchange executor and wires `A2AClient → AgentRelayService(client, exchange_executor) → AgentObserver(service, client)` with no construction cycle, and `backend/app/main.py` runs `recover_interrupted_exchanges()` at startup under `background_maintenance_enabled` beside the maintenance thread (the scheduler thread starts in T092), to make T054's `test_restart_recovery_is_invoked_once_at_boot_under_background_maintenance` pass (014-FR-006, 014-FR-007)
- [x] T071 [US2] Mirror the hand-off contract in `frontend/src/api/agentTypes.ts`, `frontend/src/api/client.ts` and `frontend/src/api/agentHooks.ts` to make T060 pass: `supporting_items`, `correlation_id`, `message_id`, `push_callback`, `acknowledgement_required`, `acknowledge_duplicate_risk`, `checkAgentRunDelivery`, and the dispatch-side run fields (014-FR-005, 014-FR-006)
- [x] T072 [P] [US2] Realise D-02-S01..S15 in `frontend/src/features/agents/AgentHandoffOverlay.tsx` to make T058 pass: real `<button>` controls, focus containment and Escape, focus restored to the invoking control, the acknowledgement checkbox gating **Send to agent**, the extension link on explicit click with an external marker, the masked push callback with its disclosure, and the `Reporting instructions` `ReviewRow` removed (the row whose label interpolates `manifest.instructions_version` and `manifest.protocol_version` and whose body renders `manifest.reporting_instructions`), with the `Destination` `ReviewRow` re-bound to `destination_interface` (014-FR-003, 014-FR-005, 014-FR-014, 014-SC-005)
- [x] T073 [US2] Realise the dispatch states D-03-S03 (both variants; **Try this hand-off again** reopens the review seeded from the run's frozen manifest and re-sends the same request identity under the `agent-handoff-<token>` key, previewing anew only when the manifest changed, so the same run ID and message ID are reused exactly when the rebuilt token equals the frozen one), D-03-S04 with its **Queued** variant and D-03-S05 with **Check again** in `frontend/src/features/agents/AgentRunSection.tsx` and `frontend/src/features/agents/agentCopy.ts` to make T059 pass (014-FR-006, 014-FR-014, 014-SC-004)
- [x] T074 [US2] Mirror the hand-off contract in `mobile/src/api/types.ts`, `mobile/src/api/client.ts` and `mobile/src/api/hooks.ts` to make the client cases of T061 and T062 pass (the same fields as T071) (014-FR-005, 014-FR-006)
- [x] T075 [P] [US2] Realise M-02-S01..S14 in `mobile/src/features/agents/HandoffSheet.tsx` to make T061 pass: fixed header, scrolling body, pinned footer, the 44pt acknowledgement row, `Pressable` controls carrying the disabled accessibility state, not swipe-dismissible in flight, the masked push callback and disclosure, the extension link on explicit tap, and the `Reporting instructions` `Field` with its `Instructions … · protocol …` caption removed (the `Destination endpoint` `Field` beside it stays, relabelled `Destination` and re-bound to `destination_interface`) (014-FR-003, 014-FR-005, 014-FR-014, 014-SC-005)
- [x] T076 [US2] Realise the dispatch states M-03-S02 (both variants), M-03-S03 with its **Queued** variant and M-03-S04 with the 44pt **Check again** row in `mobile/src/features/agents/TaskAgentSection.tsx`, `mobile/src/features/agents/AgentRunSection.tsx`, `mobile/src/agents/machine.ts` and `mobile/src/lifecycle/agentGuards.ts` to make T062 pass; **Try this hand-off again** follows the T073 identity rule (the sheet already derives the `agent-handoff-<token>` key); **Check again** is disabled offline and never queued (014-FR-006, 014-FR-014, 014-SC-004)

**Checkpoint**: User Stories 1 and 2 work independently — a Task can be handed off to either vendored runtime with one task at the agent after three replays (quickstart.md §1 steps 2–3, §3), and an ambiguous dispatch is recoverable through **Check again** without a second send.

---

## Phase 5: User Story 3 - Monitor and respond without false claims (Priority: P1)

**Goal**: Run state comes only from BrainBuddy's own versioned observations: the observer's scheduler, observation pool and control pool, backoff and reset, verified push receipt that only accelerates observation, the pure state projection on every surface, reply with task succession, cancel outcomes that never invent a capability claim, **Stopped reporting** and **Agent no longer reports this run**, and the push-token redaction (plan.md §3 and §4; FR-008, FR-009, FR-010, FR-013, FR-014, FR-015, FR-016; AC-013..AC-021, AC-028, AC-029, AC-033, AC-035; SC-003 push cases, SC-004, SC-006, SC-009).

**Independent Test**: Drive one run against the Hermes stub through working, input-required (answer it and watch the succession row), completed, failed and canceled, and against the stub with `STUB_REPLY_DELAY_SECONDS=90` (quickstart.md §2 steps 2–5), then inspect both responsive Task surfaces label for label (D-03-S06..S27, M-03-S05..S26).

### Tests for User Story 3 ⚠️

> **NOTE: Write these tests FIRST, ensure they FAIL before implementation**

- [x] T077 [US3] Write the failing observation-side cases in `backend/tests/test_agent_observer.py` with the synchronous executors and `Clock` of T054 (`014_FR_006`, `014_FR_008`, `014_FR_010`, `014_SC_006`, `014_SC_007`; AC-014, AC-019, AC-020, AC-021, AC-028, AC-033, AC-035 in docstrings): `observer.run_once(now)` selects every due run across owners; coalescing per in-flight run and one probe per unreachable connection fanned out to its runs; backoff ×2 after the reporting window capped at 10 × `observation_interval_seconds` and reset by a verified push, a reply or cancel acknowledgement or a `GET` of the run; version compare-and-set; terminal lock; TaskNotFound sets `agent_task_missing_at` and stops observation; `test_ambiguous_reply_exchange_resumes_observation_at_the_deadline`; `test_restart_during_open_reply_exchange_recovers_by_succession_evidence`; the observation half of `test_drip_feeding_agent_releases_the_worker_at_the_deadline` (a failed contact, never a state); `test_cancel_reaches_the_agent_while_all_exchange_workers_are_held` (eight held exchanges, the cancel still lands within its short timeout) and `test_owner_cancels_a_run_whose_own_exchange_holds_a_worker`; `test_no_background_thread_ever_sends_content` (scheduler, observation pool, exchange recovery and retention sweep against the recording fake emit zero content-bearing `SendMessage`); `test_observation_after_content_expiry_writes_no_agent_text`; `test_predecessor_failure_during_open_reply_exchange_does_not_lock_the_run`; `test_purge_during_observation_writes_no_row`; `test_one_owners_saturated_exchanges_never_delay_another_owners_observation`; the wake port drains an observation at once; `test_014_SC_006_delayed_agent_shows_sent_then_true_terminal_within_one_interval` (a 300 s reply, no fabricated failure, the terminal state within one 60 s interval); `CreateTaskPushNotificationConfig` for an adopted task runs on the control pool and a refusal is silent; `test_observer_scheduler_starts_and_stops_once` (`main.py` starts the `agent-observer` scheduler thread once under `background_maintenance_enabled`, `_stop_maintenance_sweeps` stops it once with the 5 s join, and nothing starts under TEST without the opt-in — the guardrail T092 cites) (014-FR-006, 014-FR-008, 014-FR-010, 014-SC-006, 014-SC-007)
- [x] T078 [US3] Write the failing observation, reply, cancel and push cases in `backend/tests/test_agent_relay_service.py` (`014_FR_008`, `014_FR_009`, `014_FR_010`, `014_FR_015`, `014_SC_003`, `014_SC_007`, `014_SC_008`; AC-013, AC-015..AC-021, AC-028, AC-029, AC-033 in docstrings): `apply_observation` aborts when the run is missing, disconnected, terminal or identifiers-expired; an identical observation refreshes `last_contact_at`/`last_observed_at` only; a differing one appends the row with its `trigger` and one `observation_accepted` audit row (`test_observation_accepted_audit_rows_are_bounded_per_run_state_and_day`); a later differing observation on a terminal run is rejected with a bounded `observation_rejected` row; last contact is the latest accepted dispatch, successful observation (including unchanged) or acknowledged reply/cancel and **Stopped reporting** overlays after the reporting window; reply sends one `SendMessage` with `messageId = command_id`, `referenceTaskIds` and the same push config, is confirmed by the returned Task/Message or a history row carrying the message id, adopts a successor task under the same correlation ID with the `task_succession` row carrying both ids and never refuses the reply, treats a foreign `contextId` as `a2a_response_invalid`, shows the agent's own reason and withdraws the control on `-32004` with no successor, keeps an ambiguous reply `unconfirmed` with the control restored for a new command id only after a later blocked observation; cancel maps `-32002` → `not_cancelable`, `-32004`/`-32601` → `unsupported`, `-32001` → `task_missing`, `-32603`/5xx/timeout/transport → `unconfirmed` keeping the control, `test_cancel_transient_error_then_success_reuses_the_command_id`, and a recorded `unsupported`/`not_cancelable` outcome replays without a second `CancelTask`; the reply/cancel barrier re-targeted from `TestExternalIoMergeRaces.test_concurrent_same_key_commands_contend_and_converge_only_with_lock` on the `BlockingA2AAgent` port; `verify_push` compares the fingerprint in constant time, honours only dispatched, non-terminal, non-disconnected runs whose connection exists, classifies a late valid push as `push_after_close`, writes one bounded `push_token_rejected` row per owner/connection/UTC day and `push_verified` on success; an over-cap task is observed through `ListTasks` with `result_availability=too_large` and never drifts into **Stopped reporting**; `agent_task_missing` withdraws reply and cancel and preserves last contact (014-FR-008, 014-FR-009, 014-FR-010, 014-FR-015, 014-SC-003, 014-SC-007, 014-SC-008)
- [x] T079 [US3] Write the failing push-route, run-route and rollout cases in `backend/tests/test_agent_relay_api.py` and the route-matrix addition in `backend/tests/test_api_contract.py` (`014_FR_008`, `014_FR_010`, `014_FR_013`, `014_FR_016`, `014_SC_003`, `014_SC_009`; AC-021, AC-029, AC-035, AC-036 in docstrings): `POST /api/a2a/push/{run_id}/{token}` in the order of `contracts/push-callback.md` — 413 on a declared or streamed body over 64 000 bytes, `test_global_push_limiter_trips_before_the_run_lookup`, unknown run → 403 with no durable row and no limiter key (`test_unknown_run_flood_leaves_limiter_memory_bounded_and_no_rows`), wrong token → 403 with one bounded `push_token_rejected` row, `test_valid_push_after_terminal_is_classified_push_after_close`, the per-run 429 reachable only for known runs, verified → 204 with `observation_trigger_pending="push"`, the observer wake and `push_verified`; forged, replayed, wrong-run, another owner's run id, malformed, after-disconnect and flood pushes with zero projection change; `test_push_rejections_are_indistinguishable_to_the_caller` (same status, body and headers, no `Retry-After`); `test_push_token_never_appears_in_logs` capturing application and `uvicorn.access` logging over a verified, a rejected and a failing exception-path push; the body never parsed for state; no session and no flag gate on the route; reply and cancel refused with 400 `agent_task_missing`, `run_content_expired`, `run_terminal`; cancel on `unconfirmed` sends `CancelTask` again with the same command id and on `unsupported`/`not_cancelable` replays the recorded outcome; `GET /agent-runs/{id}` resets the backoff; the rollout-OFF matrix continuation (observation, verified push, reply, cancel, purge and retention continue); `AgentRunSummaryResponse` with `guarantee_tier`, `cancel_outcome`, `agent_task_missing`; event rows with `trigger`, `kind`, `previous_agent_task_id`, `new_agent_task_id` (014-FR-008, 014-FR-010, 014-FR-013, 014-FR-016, 014-SC-003, 014-SC-009)
- [x] T080 [US3] Write the failing run-schema cases in `backend/tests/test_agent_schemas.py` (`014_FR_009`, `014_FR_013`, `014_FR_014`): the observation-side `AgentRunResponse` fields (`agent_task_id`, `agent_task_missing`, `cancel_outcome`, `blocked_reason`, `artifacts_summary`, `result_link_interactive` always false, `result_availability`, `last_observed_at`, `observation_interval_seconds`, `identifiers_expired`, `events[]` with `trigger`/`kind`/succession ids, `commands[]` with `delivery`/`outcome_code`), the full `primary_state_label` vocabulary including `Agent no longer reports this run`, the `AgentRunSummaryResponse` additions, and `capabilities.progress` gone (014-FR-009, 014-FR-013, 014-FR-014)
- [x] T081 [US3] Write the failing SC-004 suite in `frontend/src/features/agents/__tests__/AgentRunSection.test.tsx` and `frontend/src/features/agents/__tests__/agentCopy.test.ts` with the state-distinguishability cases named `014-SC-004` (`014-FR-009`, `014-FR-013`, `014-FR-014`, `014-FR-016`; AC-013, AC-015..AC-020, AC-028, AC-029 in the stories): `it.each` over every `primary_state_label` rendered verbatim from one projection; D-03-S06 **Accepted**, S07 **Running** with the agent's attributed status text, S08 **Needs you** with the reply box, S09 reply unconfirmed, S10 auth-required without a reply control, S11 **Agent reported complete** with inert text, artifact placeholders naming the content type, every reported address as inert text beside **Copy link** (no anchor, whatever the scheme) and the **Result too large to store** marker never rendered as **Stopped reporting**, S12 **Failed** with the reason, S13 **Rejected by agent**, S14 **Cancellation requested**, S15 **Cancelled**, S16 "Cancellation not supported by this agent." withdrawing the control while "Cancellation request unconfirmed — you can try again." keeps it, S17 **Stopped reporting**, S18 **Agent no longer reports this run** with reply and cancel withdrawn, S19 the content-expired marker, S20 **Connection disconnected**, S21 compact rows with the tier in full, S22 no chips, S23 error with correlation ID, S24 partial-failure stamp, S25 offline disabling reply and cancel through real disabled buttons, S26 two runs with frozen content, S27 **The agent continued this run in a new task** with both ids; the AC-031 case on the run surface: a run whose `agent_name` carries markup or a `javascript:` address renders it as plain text with no anchor and no auto-link; never a fabricated percentage, stage or ETA, and the Task never completed by an agent report (014-FR-009, 014-FR-013, 014-FR-014, 014-FR-016, 014-SC-004)
- [x] T082 [P] [US3] Write the failing compact-surface cases in `frontend/src/features/tasks/__tests__/TaskDetailPanel.test.tsx` and `frontend/src/features/tasks/__tests__/TaskListPage.test.tsx` (`014-FR-013`, `014-SC-004`): the compact summary shows the guarantee tier in full, the cancellation-unsupported line, the **Agent no longer reports this run** label and the needs-you tint duplicating a chip that already says "Needs you" (D-03-S21, D-03-S22) (014-FR-013, 014-SC-004)
- [x] T083 [US3] Write the failing run-client cases in `frontend/src/api/__tests__/agentHooks.test.tsx` and `frontend/src/api/__tests__/agentClient.test.ts` (`014-FR-008`, `014-FR-010`): run polling at the response's `observation_interval_seconds`, reply and cancel with stable command ids under `Idempotency-Key`, a cancel retry after `unconfirmed` reusing the same command id, and the run query invalidated by each (014-FR-008, 014-FR-010)
- [x] T084 [US3] Write the failing mobile SC-004 suites named `014-SC-004` in `mobile/src/features/agents/__tests__/TaskAgentSection.test.tsx`, `mobile/src/features/agents/__tests__/AgentRunSection.test.tsx`, `mobile/src/agents/__tests__/machine.test.ts` (label for label from the same projection; `agent_task_missing` and `cancel_outcome ∈ {unsupported, not_cancelable}` withdraw the controls, `unconfirmed` keeps cancel) and `mobile/src/lifecycle/__tests__/agentGuards.test.ts` (`it.each` over the full state enumeration; reply and cancel disabled offline and never queued, M-03-S22), and update the existing hook suites `mobile/src/api/__tests__/agentSummaryHooks.test.tsx`, `mobile/src/api/__tests__/agentRunsExpiryHooks.test.tsx` and `mobile/src/api/__tests__/relayMutationOffline.test.tsx` to the renamed fields (`014-FR-009`, `014-FR-013`, `014-FR-014`, `014-FR-016`): M-03-S05..S19 and S20..S26 including the 44pt **Copy link** on S10, the too-large marker, S24 compact rows, S25 and the S26 succession row, and the AC-031 case: a run whose `agent_name` carries markup or a `javascript:` address renders it with no `Linking` target and no auto-link (014-FR-009, 014-FR-013, 014-FR-014, 014-FR-016, 014-SC-004)

### Implementation for User Story 3

- [x] T085 [US3] Add the observation documents to `backend/app/modules/agents/domain.py` to make the domain cases of T077–T078 pass: `AgentRunEventDocument` gains `trigger`, `agent_state`, `agent_status_at`, `kind`, `previous_agent_task_id`, `new_agent_task_id`; `AgentRunCommandDocument` gains `delivery`, `outcome_code`, `agent_task_id_after` with the confirmation rules of data-model.md §4; the succession precedence and the content-expiry write rule of §2; the label precedence including **Agent no longer reports this run**; the `cancel_outcome` secondary line; the last-contact and **Stopped reporting** rules (014-FR-008, 014-FR-009, 014-FR-010, 014-FR-015)
- [x] T086 [US3] Add the observation persistence to `backend/app/modules/agents/repository.py` to make the repository cases of T077–T079 pass: due-run selection grouped for per-connection coalescing, `next_observation_at` writes with the backoff derived from `last_contact_at`, event append with `kind` and succession ids only on change, command outcome writes, the bounded `observation_accepted`, `observation_rejected`, `push_token_rejected`, `push_after_close` and `push_verified` audit rows, `push_token_fingerprint` lookup by run id (kept until identifier expiry), `observation_trigger_pending` writes, no run insert path for background threads, and the limiter eviction hooks on terminal and disconnect (014-FR-008, 014-FR-010, 014-SC-003, 014-SC-007)
- [x] T087 [US3] Complete `backend/app/modules/agents/observer.py` to make T077 pass: the `agent-observer` scheduler thread every `observation_interval_seconds`, the observation pool (`observer_workers`) issuing `GetTask {id, historyLength: 0}` under the `connector_timeout_seconds` (10 s) + 5 s deadline with a small bounded history only when the state needs text and the over-cap `ListTasks` fallback, the control pool (`control_workers`, the same 10 s + 5 s deadline) for `CancelTask` and `CreateTaskPushNotificationConfig`, coalescing per in-flight run and per unreachable connection, backoff and reset, the wake port (`Callable[[str], None]`) and queue handed to the service after construction, the reply-exchange suspension exiting at `closed`/`interrupted`/`exchange_deadline_at`, reply recovery by succession evidence, `run_once(now)` for tests, and the stop event with `shutdown(wait=False, cancel_futures=True)` joined under a 5 s timeout; scheduling, wake draining and network I/O only, no business rules (014-FR-006, 014-FR-008, 014-FR-010, 014-SC-006)
- [x] T088 [US3] Complete the observation path in `backend/app/modules/agents/service.py` to make T078 pass: `apply_observation` with the full rules (abort when missing, disconnected, terminal or identifiers-expired; the content-expiry write rule; succession precedence; `agent_task_missing_at`; bounded audit), `reply_to_run` through `SendMessage` with the command id, the same push config, succession adoption and the `-32004` withdraw rule, `cancel_run` on the control pool with the outcome mapping and the same-command-id retry, `verify_push` (constant-time fingerprint, run-state classification, wake), the last-contact and **Stopped reporting** rules, `record_run_read` resetting the backoff on `GET`, the rollout-OFF continuation of observation, push, reply and cancel, and the audit actions `run_replied`, `run_cancel_requested`, `task_succession_recorded`, `push_registered`, `push_refused` (014-FR-008, 014-FR-009, 014-FR-010, 014-FR-015, 014-FR-016, 014-SC-003)
- [x] T089 [US3] Change the run contract in `backend/app/schemas/agents.py` to make T080 pass: the observation-side `AgentRunResponse`, event and command fields, the `primary_state_label` vocabulary, `AgentRunSummaryResponse` additions and the `agent_task_missing` reason (014-FR-009, 014-FR-013, 014-FR-014)
- [x] T090 [P] [US3] Expose the observer's narrow wake port from `backend/app/api/dependencies.py` for the push route, to make the observer-wake case of T079 pass (verified push → wake port → immediate observation with `observation_trigger_pending="push"`); `get_agent_relay_service` and `require_external_agent_relay_enabled` keep their semantics (014-FR-008)
- [x] T091 [US3] Add `POST /a2a/push/{run_id}/{token}` to `backend/app/api/agents.py` to make T079 pass (no session, not flag-gated, the 64 000-byte declared and streamed cap, the contract's check order with the limiter before any database read, byte-identical opaque 403s, 204 on verification, the wake port via `Depends()`), the reply and cancel deltas, the backoff reset on `GET`, and the summary fields (014-FR-008, 014-FR-010, 014-FR-013, 014-SC-003)
- [x] T092 [US3] Wire the observer lifecycle: `backend/app/container.py` builds the three bounded executors from `AgentRelaySettings` and hands the observer's wake port to the service after construction; `backend/app/main.py` starts the scheduler thread next to the maintenance thread only under `background_maintenance_enabled` and stops it in `_stop_maintenance_sweeps` with the 5 s join, to make T077's `test_observer_scheduler_starts_and_stops_once` pass (014-FR-008, 014-SC-006)
- [x] T093 [US3] Mirror the run contract in `frontend/src/api/agentTypes.ts`, `frontend/src/api/client.ts` and `frontend/src/api/agentHooks.ts` to make T083 pass: the observation-side run fields, events and commands, the summary fields, polling at `observation_interval_seconds`, reply and cancel with stable command ids and the same-id cancel retry (014-FR-008, 014-FR-010, 014-FR-013)
- [x] T094 [US3] Realise D-03-S06..S20, S22..S27 and the compact rows of D-03-S21 in `frontend/src/features/agents/AgentRunSection.tsx` and `frontend/src/features/agents/agentCopy.ts` to make T081 pass: `canReplyToRun`/`canCancelRun` withdraw on `agent_task_missing` and `cancel_outcome ∈ {unsupported, not_cancelable}` and stay on `unconfirmed`; the **Copy link** control beside every inert reported address; the too-large marker; the succession row; every label rendered verbatim from the projection; real disabled buttons offline (014-FR-009, 014-FR-010, 014-FR-013, 014-FR-014, 014-FR-016, 014-SC-004)
- [x] T095 [P] [US3] Add the compact summary fields (`guarantee_tier` in full, `cancel_outcome`, `agent_task_missing`) to `frontend/src/features/tasks/TaskDetailPanel.tsx` and `frontend/src/features/tasks/TaskListPage.tsx` to make T082 pass (D-03-S21, D-03-S22) (014-FR-013, 014-SC-004)
- [x] T096 [US3] Mirror the run contract in `mobile/src/api/types.ts`, `mobile/src/api/client.ts` and `mobile/src/api/hooks.ts` to make the client cases of T084 pass (the same fields as T093) (014-FR-008, 014-FR-010, 014-FR-013)
- [x] T097 [US3] Add the new conditions and withdrawn controls to `mobile/src/agents/machine.ts` and `mobile/src/lifecycle/agentGuards.ts` to make the machine and guard cases of T084 pass: labels verbatim from the projection, reply and cancel withdrawn on `agent_task_missing` and unsupported/not-cancelable outcomes, kept on `unconfirmed`, disabled offline with the disabled accessibility state and never queued (M-03-S22) (014-FR-009, 014-FR-013, 014-FR-014, 014-FR-016)
- [x] T098 [US3] Realise M-03-S05..S19 and S20..S26 with the compact rows of M-03-S24/S25 in `mobile/src/features/agents/TaskAgentSection.tsx` and `mobile/src/features/agents/AgentRunSection.tsx` to make T084 pass: the 44pt **Copy link** row on S10 with the address as inert text, the too-large marker, the S26 succession row, `Pressable` controls with the disabled accessibility state (014-FR-009, 014-FR-010, 014-FR-013, 014-FR-014, 014-FR-016, 014-SC-004)

**Checkpoint**: User Stories 1–3 are independently functional — a run against the Hermes stub can be watched through every agent-reported state, answered, cancelled and observed after a stub restart (quickstart.md §2 steps 2–5) with one projection on web and iOS.

---

## Phase 6: User Story 4 - Disconnect and retain only bounded evidence (Priority: P2)

**Goal**: Disconnect destroys the credential together with the discovered card summary and its fingerprint, stops observation and push registration for the connection's runs and keeps bounded redacted history; the retention tiers hold on every read before the sweep and the sweep survives an unparseable row; account purge removes everything (plan.md §3 retention clauses and §7; FR-016 with 007 FR-015/FR-016; AC-022..AC-025; SC-007).

**Independent Test**: Disconnect a connection after recent reauthentication (D-01-S23 / M-01-S21), inspect the existing run history, advance the retention clocks past 30 and 90 days, and run the account-purge flow (quickstart.md §6).

### Tests for User Story 4 ⚠️

> **NOTE: Write these tests FIRST, ensure they FAIL before implementation**

- [x] T099 [US4] Write the failing disconnect, retention and purge cases in `backend/tests/test_agent_relay_service.py` (`014_FR_016`, `014_SC_007`; AC-022..AC-025 in docstrings): disconnect after reauthentication destroys the credential, `card` and `card_fingerprint` together, stops observation (`next_observation_at=None`) and push registration for the connection's runs (no later `CreateTaskPushNotificationConfig`, limiter keys evicted), refuses new hand-offs and replies, freezes live runs as **Connection disconnected** without claiming cancellation and preserves bounded redacted history; the secret never appears in any connection, run, audit, export, log or error view; `test_expire_due_content_skips_and_logs_an_unparseable_row` (a correlation id and no content in the log line, every other owner's content still expires), `test_expired_but_unswept_run_reads_with_no_agent_text` (the sweep deliberately not run), `test_run_id_is_retained_with_the_run_row_until_purge_and_identifiers_expire_at_90_days` (the honest SC-007 statement), content written after expiry re-erased on the next pass, the live connection's card summary and fingerprint surviving every sweep, audit rows gone at 90 days, `run_retention_sweep` calling `expire_due_identifiers` and `purge_expired_audit`, `delete_all_for_owner` leaving no row for the owner and every row for another owner, and the `relay/relay.json` export without `credential`, `push_token_fingerprint` or `card_fingerprint` (014-FR-016, 014-SC-007)
- [x] T100 [US4] Write the failing retention cases in `backend/tests/test_agent_repository.py` (`014_FR_016`, `014_SC_007`): `expire_due_content` nulls the six 007 fields plus `blocked_reason`, `artifacts_summary`, `result_availability` and the status text with a selection predicate that re-detects a non-null value in any content column, and skips and logs an unparseable row instead of rolling back the transaction; the new `expire_due_identifiers` nulls `message_id`, `agent_task_id`, `interface_url`, `card_fingerprint`, `push_token_fingerprint`, `agent_task_id_after` and purges event rows at `identifiers_expire_at` while the run row and its id stay; `disconnect_connection` erases `card`, `card_fingerprint` and the credential and writes `disconnect_reason=owner`; `delete_all_for_owner` covers every table (014-FR-016, 014-SC-007)
- [x] T101 [P] [US4] Rewrite the relay fixture in `backend/tests/test_account_export.py` (`014_SC_007`; AC-025 in the docstring): replace the `inbound_secret` fixture at lines 100–102 with a 014 connection and dispatched run, and assert the `relay/relay.json` member carries the run id and coarse metadata while excluding `credential`, `push_token_fingerprint` and `card_fingerprint` (014-FR-016, 014-SC-007)
- [x] T102 [US4] Write the failing D-01-S23 cases in `frontend/src/features/agents/__tests__/AgentSettingsPage.test.tsx` (`014-FR-016`; AC-022 in the story): the disconnect confirmation names the credential, the discovered card summary and its fingerprint as erased together and states that work the agent already accepted is not cancelled, requires the password, reads safe-then-destructive, traps focus with **Disconnect** as the last control, is not dismissed by Escape alone, and restores focus to the invoking **Disconnect** (014-FR-016)
- [x] T103 [P] [US4] Write the failing mobile suite `mobile/src/features/agents/__tests__/DisconnectSheet.test.tsx` for M-01-S21 (`014-FR-016`; AC-022 in the story): the destructive action sits below **Keep it connected**, the password is required, the same erasure sentence as D-01-S23 is shown, the disabled accessibility state applies while in flight, and a stray gesture does not decide (014-FR-016)

### Implementation for User Story 4

- [x] T104 [US4] Extend `domain.project_run_for_access` in `backend/app/modules/agents/domain.py` to null `blocked_reason`, `artifacts_summary`, `result_availability` and the agent status text beside the 007 fields on every read path, so due content is inaccessible before the sweep (T099's unswept-read case) (014-FR-016, 014-SC-007)
- [x] T105 [US4] Extend the sweeps in `backend/app/modules/agents/repository.py` to make T100 pass: `expire_due_content` nulls the 014 columns with the re-detecting predicate and skips and logs an unparseable row (correlation id, no content) without aborting the transaction, the new `expire_due_identifiers`, `disconnect_connection` erasing `card`, `card_fingerprint` and the credential with `disconnect_reason=owner`, the `relay/relay.json` composition of `export_owner_data` with its exclusions, and `delete_all_for_owner` over every table (014-FR-016, 014-SC-007)
- [x] T106 [US4] Complete disconnect and retention in `backend/app/modules/agents/service.py` to make T099 pass: `disconnect_connection` stops observation and push registration for the connection's runs (limiter keys evicted, `next_observation_at=None`, `connection_disconnected_at` stamped), refuses hand-offs and replies, `run_retention_sweep` runs the hardened content sweep, `expire_due_identifiers` (audit `identifiers_expired`), `purge_expired_audit` and `purge_all_expired_idempotency`, export composition never carries a secret (014-FR-016, 014-SC-007)
- [x] T107 [US4] Realise D-01-S23 in `frontend/src/features/agents/AgentSettingsPage.tsx` to make T102 pass: the erasure sentence naming the credential, card summary and fingerprint, the not-cancelled sentence, password, safe-then-destructive order, the focus trap ending on **Disconnect**, dismissal only through **Keep it connected**, focus restored (014-FR-016)
- [x] T108 [P] [US4] Realise M-01-S21 in `mobile/src/features/agents/DisconnectSheet.tsx` to make T103 pass: destructive action below the safe one, password, the same erasure sentence, `Pressable` disabled accessibility state in flight (014-FR-016)

**Checkpoint**: All four user stories are independently functional; the removal of the bespoke wire, the reference-runtime conformance, the product E2E stories, the documentation and the writer gates remain.

---

## Phase 7: Polish & Cross-Cutting Concerns

**Purpose**: Remove the bespoke wire (research.md Decision F, one ordered pass per shared file, after every a2a/observer task on that file), prove conformance against the two unmodified reference runtimes, ship the product E2E stories and fixtures, update the documentation, and pass the writer gates. The attended Hermes live run is a human release step recorded in `docs/external-agent-relay-release.md`, never a task here.

### Removal of the bespoke wire (sequenced after T044/T067/T088/T106 on the same files; never `[P]` against them)

- [x] T109 Remove the bespoke-wire tests first so every later deletion is observed by a green suite: delete `backend/tests/test_agent_connector.py` (its egress-bound cases moved in T016); strip from `backend/tests/test_agent_relay_service.py` the signing-secret rotation classes (pre-014 lines 3907–4075), the inbound-event classes (1632–1988, 3264–3457, 3845, 5097–5133) and every `inbound_signing_secret` assertion; from `backend/tests/test_agent_relay_api.py` the event-route and body-cap cases (1123–1330, 1418–1650) that T079's push-route cases superseded; from `backend/tests/test_agent_schemas.py` the reporting-contract cases; from `backend/tests/test_agent_repository.py` the event-id cases; from `backend/tests/test_agent_secrets.py` the inbound-signing and signing-receipt purpose cases; from `backend/tests/test_api_contract.py` exactly the two removed-route entries — `("/api/agent-events", "post")` and `("/api/agent-connections/{connection_id}/signing-secret", "post")` — and nothing else, since the connection, run and task-run entries around them are kept routes that T055/T079 extend; from `backend/tests/test_config.py` the callback-URL cases (87–115) — leaving T020's route-404 and import-absence cases as the only assertions about the bespoke wire (014-FR-012, 014-SC-010)
- [ ] T110 Remove the bespoke ingest path from `backend/app/modules/agents/service.py` in its last ordered pass: `rotate_signing_secret`, `_signing_secret_response`, `_replayed_signing_secret`, `_open_receipt`, `_authenticate_event_request`, `_validate_authenticated_event`, `_event_run`, `_event_updates`, `_event_summary`, `_apply_authenticated_event`, `ingest_event`, `_reporting_instructions`, `EVENT_FRESHNESS_WINDOW`, `MAX_EVENT_BYTES`, `INBOUND_SECRET_BYTES`, `EventRejected`, the `ConnectorPort` constructor port and every import of `connector` (014-FR-012, 014-SC-010)
- [ ] T111 Remove the envelope from `backend/app/modules/agents/domain.py` in its last ordered pass: the event envelope models, `AGENT_EVENT_ENVELOPE_ADAPTER`, `REPORTING_INSTRUCTIONS_VERSION`, `PROTOCOL_VERSION = "2026-08-09"` (the manifest's `protocol_version` has been `"1.0"` since T064), `AgentConnectionDocument.inbound_secret` and `AgentCapabilities.progress`; `AgentReportingContract` stays as the frozen placeholder of T031 (014-FR-012, 014-SC-010)
- [ ] T112 Remove the event-id store from `backend/app/modules/agents/repository.py` in its last ordered pass: the `agent_event_ids` table, `consume_event_id`, `purge_expired_event_ids` and the inbound-signing branches of `live_sealed_key_ids` (pre-014 lines 1185–1263); the ledger, the migration and `delete_all_for_owner` are unaffected (014-FR-012, 014-SC-010)
- [ ] T113 Remove `AAD_PURPOSE_INBOUND_SIGNING` and `AAD_PURPOSE_SIGNING_RECEIPT` and their callers from `backend/app/modules/agents/secrets.py` in its last ordered pass; the outbound-credential purpose and the push-token helper of T063 stay (014-FR-012, 014-SC-010)
- [ ] T114 Remove the routes `POST /agent-connections/{id}/signing-secret` and `POST /agent-events` from `backend/app/api/agents.py` and the schemas `AgentConnectionSecretResponse`, `AgentConnectionCreatedResponse` (superseded by the plain response of T045), `AgentConnectionSigningSecretResponse`, `AgentConnectionRotateSigningSecretRequest`, `AgentReportingContractResponse` and `AgentEventIngestResponse` from `backend/app/schemas/agents.py`, so the removed routes answer 404 as T020 asserts (014-FR-012, 014-SC-010)
- [ ] T115 Delete `backend/app/modules/agents/connector.py`, remove `AGENT_EVENTS_PATH` and `agent_relay_callback_url` from `backend/app/core/config.py` (T012 added their replacements), and drop the synthetic signing-secret allowlist entries (lines 57–64) from `.gitleaks.toml`; T020's import-absence case turns green here (014-FR-012, 014-SC-010)
- [ ] T116 Remove the bespoke surfaces from both clients (the last ordered pass on the shared client modules after T093/T096, never `[P]` against them): delete `mobile/src/features/agents/SigningSecretSheet.tsx`, `mobile/src/features/agents/ReplaceSigningSecretSheet.tsx`, `mobile/src/api/__tests__/agentSecretSettlement.test.tsx` and `mobile/src/features/agents/__tests__/signingSecretRecovery.test.tsx`; strip the signing-secret and reporting types from `frontend/src/api/agentTypes.ts`, `rotateAgentSigningSecret` from `frontend/src/api/client.ts` and its cases from `frontend/src/api/__tests__/agentClient.test.ts`; strip the signing-secret types, calls and hooks from `mobile/src/api/types.ts`, `mobile/src/api/client.ts` and `mobile/src/api/hooks.ts`, the `inbound_signing_secret` fixtures from `mobile/src/test/agentFixtures.ts` and `mobile/src/test/fakeBackend.ts`, and any remaining sheet import from `mobile/src/app/agents/index.tsx` (014-FR-012, 014-SC-010)
- [ ] T117 Prove SC-010 end to end: `cd backend && pytest tests/test_agent_repository_migration.py -v` is fully green (migration, audit and count, idempotent startup, route 404s, preview refusal, import absence), the quickstart.md §9 grep `grep -rn "agent-events\|inbound_signing_secret\|AGENT_EVENT_ENVELOPE_ADAPTER\|modules.agents.connector" backend/app frontend/src mobile/src; test $? -eq 1` finds nothing in product code, and a seeded bespoke row renders as D-01-S21 / M-01-S19 **Superseded wire contract** (014-FR-012, 014-SC-010)

### Reference runtimes and product E2E (gated on T023–T098)

- [ ] T118 [P] Write and pass `backend/tests/test_agent_a2a_reference_helloworld.py` (`014_FR_017`, `014_SC_001`, `014_SC_002`; AC-001, AC-009, AC-016, AC-026 in docstrings): a pre-bind availability check on `127.0.0.1:9999` that fails — never skips — with a message naming the occupant; `python __main__.py` started as a subprocess (cwd `backend/vendor/a2a_helloworld/`, `HOME` a temp dir) with a 30 s startup wait on the card URL (fail, never skip, when it elapses) and guaranteed teardown through `request.addfinalizer` (kill + wait, also on assertion failure or timeout); the container built with `BRAIN_BUDDY_AGENT_ALLOW_PRIVATE_DESTINATIONS=1`; connect → test (best-effort, no push) → preview with the one-time acknowledgement → dispatch ×3 → **Sent** then **Agent reported complete** inside the exchange with the sample's result text → exactly one task through `ListTasks(contextId)`; cancel on the completed run withdrawn and the sample's cancel refusal asserted as `-32004` (UnsupportedOperation → `cancel_outcome=unsupported`); should the pinned sample answer `-32603` instead, the documented fallback of `contracts/a2a-wire.md` applies (`cancel_outcome=unconfirmed`, control kept) and the assertion is corrected to the observed code, never widened to accept both (014-FR-017, 014-SC-001, 014-SC-002)
- [ ] T119 [P] Write and pass `backend/tests/test_agent_a2a_reference_hermes.py` (`014_FR_017`, `014_SC_001`, `014_SC_002`, `014_SC_006`; AC-010, AC-014, AC-015, AC-018, AC-020, AC-021, AC-028 in docstrings): `backend/vendor/hermes_a2a/run_stub.py` bound to port 0 with the bound port read back; the legacy card shape parsed (bearer, push, streaming); a wrong bearer → invalid credentials with nothing echoed; `test_014_SC_002_three_replays_create_one_task_at_the_unmodified_hermes_stub` — three identical confirmations (the same manifest token and `Idempotency-Key`) followed by `ListTasks(contextId)` at the stub asserting exactly one task and one Task-linked run, the second of SC-002's three reference replays beside T118's helloworld case and T052's extension fake; a delayed dispatch → **Sent**/**Running** without a fabricated failure, then **Agent reported complete** within one observation interval and at once on the stub's push (`trigger: push`); `ask me` → **Needs you** → reply → the succession row with both task ids and the run's single correlation ID; cancel while delaying → **Cancellation requested** → **Cancelled**; a stub restart → **Agent no longer reports this run** with last contact kept; a stub without `A2A_BEARER_TOKEN` → `agent_card_changed` on preview (014-FR-017, 014-SC-001, 014-SC-002, 014-SC-006)
- [ ] T120 [P] Add the compose fixture images and stack: `scripts/fixtures/a2a-helloworld/Dockerfile` (`python:3.11-slim`, `pip install a2a-sdk[http-server]==1.1.2 uvicorn`, the vendored sample) and `scripts/fixtures/hermes-a2a/Dockerfile` (the vendored plugin, the stub gateway and `run_stub.py` with `A2A_HOST=0.0.0.0`, `A2A_PORT=9900`, `A2A_BEARER_TOKEN`, `A2A_PUBLIC_URL`); services `a2a-helloworld` (`network_mode: "service:backend"`, because the sample binds `127.0.0.1:9999`) and `hermes-a2a` (reachable as `http://hermes-a2a:9900`) under profile `agents` in `compose.yaml`, with `BRAIN_BUDDY_AGENT_ALLOW_PRIVATE_DESTINATIONS=1` and `BRAIN_BUDDY_PUBLIC_BASE_URL` on the backend service; `scripts/run_playwright_e2e.sh` enables the profile and toggles `external_agent_relay` ON through the operator API (014-FR-017, 014-SC-001)
- [ ] T121 [P] Extend the `e2e` job in `.github/workflows/ci.yml` to build the two fixture images through the same buildx cache helper as the app images before `make test-e2e`, and confirm `python3 scripts/validate_ci_artifacts.py workflow` accepts the new build steps (ASK-class path under the `deploy-and-ci` rules) (014-FR-017, 014-SC-001)
- [ ] T122 Write and pass the Playwright stories in `frontend/tests/e2e/agent-relay.compose.spec.ts` under the `External agent relay` epic (the T009 rule): `A2A hand-off to the helloworld sample` (`014-SC-005`, `014-SC-001` — the desktop-width review D-02-S01/S02/S13/S14 end to end to **Agent reported complete** D-03-S11), `A2A hand-off to the Hermes stub` (`014-SC-004`, `014-SC-006` — working, input-required, completed, failed and canceled through D-03-S07/S08/S09/S11/S12/S14/S15 and the succession row S27 on the product surfaces), `A2A replay creates one task` (`014-SC-002` — three confirmations of one review against the `hermes-a2a` fixture service of T120, one task at the agent: the run projection shows one run with one `agent_task_id` and the stub's `ListTasks(contextId)`, called through the fixture's JSON-RPC endpoint, lists exactly one task) and `A2A security rejections` (`014-SC-003` — invalid credentials D-01-S12, private destination D-01-S18, a forged push, **Agent changed** D-01-S20, each with zero state change) via `data-testid` selectors against the compose stack of T120 (014-SC-001, 014-SC-002, 014-SC-003, 014-SC-004, 014-SC-005)
- [x] T123 [P] Add the per-epic section **External agent relay product matrix** to `docs/e2e-acceptance-charter.md` under "Browser E2E acceptance scenarios", beside the unchanged native table, listing the four stories with their success criteria and fixture services; the native-story validator is unchanged (014-SC-001, 014-SC-004)

### Documentation

- [x] T124 [P] Add the relay rows to `docs/data-retention.md`: the 30-day content tier (`expire_due_content`), the 90-day identifiers tier (`expire_due_identifiers`: agent task ids, message ids, push token fingerprint) with the sentence that the run id — the correlation ID — stays with the run row as coarse metadata until account purge, the 90-day audit rows (`purge_expired_audit`), the live connection's card summary and fingerprint retained for the connection's lifetime and erased by `disconnect_connection` together with the credential, the `relay/relay.json` export member with its exclusions, and the disclosed agent-side copy of the callback URL; verified by the repository-contract test `test_014_SC_007_data_retention_doc_names_the_relay_tiers_and_sweeps` added beside T099 in `backend/tests/test_agent_relay_service.py`, which reads `docs/data-retention.md` and asserts the four sweep or erasure names (`expire_due_content`, `expire_due_identifiers`, `purge_expired_audit`, `disconnect_connection`), the run-id sentence and the `relay/relay.json` member (the pattern of `backend/tests/test_admin_deploy_contract.py`) (014-FR-016, 014-SC-007)
- [x] T125 [P] Add the matching user-facing paragraph to `frontend/src/pages/PrivacyPolicyPage.tsx` (30-day content, 90-day identifiers with the honest run-id sentence, 90-day audit, the card summary kept for the connection's lifetime and erased on disconnect, the per-run callback address the agent keeps) with `LAST_UPDATED` bumped, and extend `frontend/src/pages/__tests__/PrivacyPolicyPage.test.tsx` to assert the new text and date (014-FR-016, 014-SC-007)
- [x] T126 [P] Add one line to `docs/auth.md` after the 404-for-wrong-owner rule: the A2A push callback is the one route that answers 403 rather than 404, and its per-run 429 is a deliberate, run-id-gated existence signal accepted because run ids are unguessable and the token authorises only an observation BrainBuddy would perform anyway (the sentence FR-008 and SC-003 now ratify); verified by a grep assertion beside `test_push_rejections_are_indistinguishable_to_the_caller` in `backend/tests/test_agent_relay_api.py` (T079) that `docs/auth.md` carries the 403-not-404 sentence and the per-run 429 sentence (014-FR-008, 014-SC-003)
- [x] T127 [P] Update `docs/external-agent-relay-release.md`: rewrite step 5 for the A2A smoke; add the rollout gate — `external_agent_relay` MUST NOT be turned ON for any user until the refreshed iOS build is the only distributed build (TestFlight and App Store phased release complete), the Principle III migration note of `contracts/api-deltas.md` §Compatibility; the pre-deploy step counting bespoke connection rows in production (payloads without a `wire` key) before the 014 image starts; the true rollback consequences and their verified mitigation from plan.md "Risks and rollback"; and the "Hermes live evidence" section — approval-gated like `verify-live`, never CI, never a subagent — whose evidence is the closed field list (agent name, version, protocol version, declared capabilities, guarantee tier, correlation ids, run id, the resulting `primary_state_label` sequence) and never the agent address or `interface_url`, the credential, the push token or its fingerprint, the card description or skill text, the Task title, details or supporting items, artifacts or model output; verified by a grep assertion in `backend/tests/test_agent_repository_migration.py` (T020/T117) that the document names the pre-deploy bespoke-row count step, the rollout-gate sentence and the closed live-evidence field list, and by the ASK reviewer of PR #192 reading the runbook (review.md, compensating measures) (014-FR-017, 014-FR-016, 014-FR-012)

### Writer gates

- [ ] T128 Run `python3 scripts/check_requirement_coverage.py specs/014-a2a-relay-wire-contract` and make it trace every requirement — 014-FR-001 through 014-FR-017 and 014-SC-001 through 014-SC-010 — to a named test, adding the missing feature-qualified marker to the covering test rather than a vacuous new one; because the tracer accepts any one test per id, also record for the acceptance auditor the per-clause covering tests of the three bundled requirements, without splitting their ids — **FR-006**: identifier reservation, one message with metadata, the outcome table and `context_id_honoured` → `test_agent_relay_service.py` (T052, incl. `test_014_SC_002_three_replays_create_one_task`); **Queued** / **Sent** / **Delivery unconfirmed** / **Not sent** → `test_agent_relay_api.py` (T055 label cases) and `test_agent_observer.py` (T054); lookup-before-resend, **Check again**, the eight refusal reasons, the `first_dispatch_at` reauthentication rule, the pinned interface and the SC-008 barriers → `test_agent_relay_service.py` (T053); no background send → `test_agent_observer.py::test_no_background_thread_ever_sends_content` (T077); restart recovery → `test_agent_observer.py` (T054 `test_restart_recovery_*`); rollout-OFF lookup-only → `test_agent_relay_api.py::test_rollout_off_check_delivery_looks_up_but_never_resends` (T055). **FR-010**: reply with a stable command id, dedup, unconfirmed-until-acknowledged, task succession, the `-32004` withdraw rule and the cancel outcome mapping → `test_agent_relay_service.py` (T078, incl. `test_cancel_transient_error_then_success_reuses_the_command_id`); the suspension, its exit and the predecessor terminal report → `test_agent_observer.py::test_ambiguous_reply_exchange_resumes_observation_at_the_deadline`, `::test_predecessor_failure_during_open_reply_exchange_does_not_lock_the_run`, `::test_restart_during_open_reply_exchange_recovers_by_succession_evidence` (T077); the control pool → `test_agent_observer.py::test_cancel_reaches_the_agent_while_all_exchange_workers_are_held` (T077); the cancellation disclosure → `test_agent_relay_service.py` (T051); succession end to end → `test_agent_a2a_reference_hermes.py` (T119). **FR-016**: inert result links → `test_agent_a2a_mapping.py` (T014), `test_agent_egress.py` (T016) and `AgentRunSection.test.tsx` / `TaskAgentSection.test.tsx` (T081/T084); inert card text → `test_agent_relay_api.py::test_card_text_is_returned_verbatim_and_bounded` (T035) and the AC-031 cases of T038/T039/T058/T061/T081/T084; the 30-day, 90-day and run-id retention statements → `test_agent_relay_service.py::test_run_id_is_retained_with_the_run_row_until_purge_and_identifiers_expire_at_90_days` (T099) and `test_agent_repository.py` (T100); the card summary's connection lifetime and disconnect stopping observation and push → `test_agent_relay_service.py` (T099); no secrets in logs → `test_agent_relay_api.py::test_push_token_never_appears_in_logs` (T079) and the Decision K log-field cases of T018; rollout-OFF continuation → the `test_agent_relay_api.py` rollout-OFF matrix (T035/T079) (014-FR-001, 014-FR-017, 014-SC-001, 014-SC-010)
- [ ] T129 Run `make verify-all` (check-specs, validate-ci, backend with its coverage floor and the Allure taxonomy validator, frontend, mobile, e2e) green in the worktree with the isolation values of T001; keep `backend/coverage-floor.json`, `frontend/coverage-floor.json` and `mobile/coverage-floor.json` ratcheting upward only with no coverage suppression; walk the quickstart.md §1–§7 and §9 scenarios once against the vendored runtimes; confirm the ASK landing class with `python3 scripts/classify_path_risk.py` (014-SC-001, 014-SC-003, 014-SC-004, 014-SC-005, 014-SC-007, 014-SC-008, 014-SC-009, 014-SC-010)
- [ ] T130 Confirm the CHK051 copy decision already recorded and ticked at the analyze stage in `specs/014-a2a-relay-wire-contract/checklists/a2a-wire.md` — "Test again shortly." is rendered when `retry_after_seconds` is null — against what T048 (web) and T050 (iOS) actually render, and amend the checklist note only if the rendered string differs (014-FR-002)
- [ ] T131 Freeze the candidate and write the local, uncommitted writer receipt conforming to `.specify/templates/pre-freeze-receipt.schema.json` — contract `brainbuddy.pre-freeze-writer-receipt/v1`, the full lowercase implementation SHA, the changed-path inventory, and the gates `writer.tests`, `writer.verify_all`, `writer.path_classification` and `writer.diff_review` each `PASS` with the exact command, observation and evidence — run `python3 scripts/validate_pre_freeze_receipt.py <receipt> --sha <full-lowercase-sha>`, and tick CHK052 in `specs/014-a2a-relay-wire-contract/checklists/a2a-wire.md`; independent review, QA, exact-SHA CI, landing, deploy, the attended Hermes run and rollout remain post-freeze human obligations under ADR-0008 (writer gate: the ADR-0008 pre-freeze receipt; traced as 014-SC-010 because the receipt's changed-path inventory and `writer.diff_review` are the evidence that no bespoke path remains)

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — T001 first, then T002–T010; T004–T010 run in parallel with each other once T002/T003 have landed the `backend/pyproject.toml` edits
- **Foundational (Phase 2)**: Depends on Setup — BLOCKS all user stories. T011–T022 precede T023–T033 — the failing tests and the fakes are `[P]` except T018 (after the T015 fakes it imports), T019 and T022 (the first passes on the rewritten `test_agent_relay_api.py` / `test_agent_relay_service.py` suites); within the implementation half the order is types → card → egress deadline → client → mapping → limiter → redaction → repository → domain → ports → extension doc
- **User Stories (Phases 3–6)**: All depend on Foundational. They ship in priority order; the backend lane is sequential per file, while the frontend and mobile lanes of a story start as soon as that story's schema task (T045, T068, T089) has landed
- **Polish (Phase 7)**: The removal lane (T109–T117) depends on every a2a/observer pass on the same files (T044, T067, T088, T106 and their domain/repository siblings) — it is the last ordered pass per file; the reference-runtime tests (T118, T119) and the Playwright stories (T122) are gated on the complete §1–§4 pipeline (T023–T098); the docs (T123–T127) may start any time after their facts are implemented; the writer gates (T128–T131) are last

### User Story Dependencies

- **User Story 1 (P1, MVP)**: Starts after Foundational — no dependency on any other story. Independently testable with the two vendored runtimes and no dispatch
- **User Story 2 (P1)**: Starts after US1's connection contract (T042–T047, T049): a tested, non-stale connection is the precondition of every hand-off. Its backend tests seed a ready connection through the service, so it is independently testable once US1's backend has landed
- **User Story 3 (P1)**: Starts after US2's dispatch path (T064–T070): an observation needs a dispatched run. Its tests seed dispatched runs, so it is independently testable once US2's backend has landed
- **User Story 4 (P2)**: Depends only on US1 (a connection to disconnect) and Foundational (the columns and sweeps); its tests seed runs directly. It may be built in parallel with US2/US3 by a second developer, subject to the shared-file rule below
- **Independent stories**: US1 stands alone; US4 is independent of US2 and US3; US2 → US3 is the only hard chain

### Within Each User Story

- Tests MUST be written and observed failing before implementation (the `feature-implementer` rule: an import error is not a meaningful failure)
- Backend schemas change before any client depends on the new shape (constitution Principle III): T045 → T047/T049, T068 → T071/T074, T089 → T093/T096
- Models before services, services before routes, routes before clients, story complete before the next priority

### Per-file ordering (the plan's lane rule)

- `backend/app/modules/agents/service.py`: T032 → T044 → T067 → T088 → T106 → T110 (removal last)
- `backend/app/modules/agents/domain.py`: T031 → T042 → T064 → T085 → T104 → T111 (removal last)
- `backend/app/modules/agents/repository.py`: T030 → T065 → T086 → T105 → T112 (removal last)
- `backend/app/modules/agents/headers.py`: T043 only (additions and removals in one pass)
- `backend/app/modules/agents/secrets.py`: T063 → T113 (removal last)
- `backend/app/modules/agents/observer.py`: T066 → T087
- `backend/app/schemas/agents.py` and `backend/app/api/agents.py`: T045/T046 → T068/T069 → T089/T091 → T114 (removal last)
- `backend/app/container.py` and `backend/app/main.py`: T032 → T070 → T092
- `backend/tests/test_agent_relay_service.py`: T022 → T034 → T051 → T052 → T053 → T078 → T099 → T109; `backend/tests/test_agent_relay_api.py`: T019 → T035 → T055 → T079 → T109
- `backend/tests/test_agent_observer.py`: T054 → T077; `backend/tests/test_agent_schemas.py`: T036 → T056 → T080 → T109; `backend/tests/test_agent_repository.py`: T021 → T100 → T109; `backend/tests/test_api_contract.py`: T055 → T079 → T109; `backend/tests/test_config.py`: T011 → T109; `backend/tests/test_agent_secrets.py`: T057 → T109; `backend/tests/a2a_fakes.py`: T015, before T018 (which imports it)
- `backend/app/core/config.py`: T012 → T115 (removal last); `backend/pyproject.toml`: T002 → T003
- `frontend/src/api/agentTypes.ts`, `client.ts`, `agentHooks.ts`: T047 → T071 → T093 → T116 (removal last); `frontend/src/api/__tests__/agentClient.test.ts` and `agentHooks.test.tsx`: T037 → T060 → T083 → T116; `frontend/src/features/agents/AgentSettingsPage.tsx`: T048 → T107 and its suite: T038 → T102; `AgentRunSection.tsx` / `agentCopy.ts`: T073 → T094 and their suites: T059 → T081
- `mobile/src/api/types.ts`, `client.ts`, `hooks.ts`: T049 → T074 → T096 → T116 (removal last); `mobile/src/test/agentFixtures.ts` and `fakeBackend.ts`: T040 → T116; `mobile/src/api/__tests__/agentClient.test.ts` and `hooks.test.tsx`: T040 → T062; `mobile/src/app/agents/index.tsx`: T050 → T116; `mobile/src/agents/machine.ts` and `mobile/src/lifecycle/agentGuards.ts`: T076 → T097; `mobile/src/features/agents/TaskAgentSection.tsx` / `AgentRunSection.tsx`: T076 → T098 and their suites with `machine.test.ts`: T062 → T084

### Parallel Opportunities

- Setup: T004–T010 together after T003
- Foundational: T011, T013–T017, T020, T021 together; T018 after T015; T019 and T022 open their suite chains (different files from each other, so they still run beside the rest); T033 beside the implementation half
- Every story: the frontend lane and the mobile lane run beside the backend lane once the story's schema task has landed; inside a lane, story-specific component tasks (`[P]`) run beside the shared-module tasks
- Polish: T116 beside T110–T115 (different files, yet not `[P]`: it is the last ordered pass on the shared client modules after T093/T096); T118–T121 and T123–T127 together once T098 has landed; T122 after T120/T121

---

## Parallel Example: User Story 1

```bash
# After T033 (Foundational complete), launch the failing tests that touch disjoint files:
Task: "T039 mobile AddConnectionSheet.test.tsx and ConnectionCard.test.tsx"
Task: "T041 mobile/integration/run.ts connection-test contract"
# ...while T034–T038 (shared backend and web suites) proceed in the backend/web lane.

# After T045 (connection schema) has landed, the two client lanes run beside the backend routes:
Task: "T047 → T048 frontend connection types, client, hooks and AgentSettingsPage (D-01)"
Task: "T049 → T050 mobile connection types, client, hooks, AddConnectionSheet and ConnectionCard (M-01)"
```

## Parallel Example: User Story 2

```bash
# Failing tests on story-specific files, beside the shared-suite tasks T051–T057:
Task: "T058 frontend AgentHandoffOverlay.test.tsx (D-02, 014-SC-005)"
Task: "T061 mobile HandoffSheet suites (M-02, 014-SC-005)"

# After T068 (manifest and dispatch schema):
Task: "T072 frontend AgentHandoffOverlay.tsx"
Task: "T075 mobile HandoffSheet.tsx"
# ...while T069/T070 (routes, container, main) land in the backend lane.
```

## Parallel Example: User Story 3

```bash
# Failing tests on story-specific files, beside T077–T081, T083, T084:
Task: "T082 frontend TaskDetailPanel.test.tsx and TaskListPage.test.tsx (D-03-S21/S22)"

# After T089 (run schema):
Task: "T090 api/dependencies.py wake port"
Task: "T095 frontend TaskDetailPanel.tsx and TaskListPage.tsx"
# ...while T091/T092 (push route, observer lifecycle) land in the backend lane and
# T093/T094 (web) and T096–T098 (iOS) proceed in their lanes.
```

## Parallel Example: User Story 4

```bash
# Failing tests on story-specific files, beside T099, T100 and T102:
Task: "T101 backend/tests/test_account_export.py relay/relay.json member"
Task: "T103 mobile DisconnectSheet.test.tsx (M-01-S21)"

# Implementation:
Task: "T108 mobile DisconnectSheet.tsx"
# ...while T104–T106 (domain, repository, service retention passes) and T107 (web D-01-S23) proceed.
```

---

## Implementation Strategy

### MVP First (User Story 1, then User Story 2)

1. Complete Phase 1: Setup (worktree, dependencies, vendored runtimes, hygiene, taxonomy, `.env.example`)
2. Complete Phase 2: Foundational (protocol surface, deadline, limiter, redaction, columns, migration, ports, extension doc) — CRITICAL, blocks all stories
3. Complete Phase 3: User Story 1 — connect and test both vendored runtimes
4. **STOP and VALIDATE**: quickstart.md §1 step 1 and §2 step 1 on web and iOS
5. Complete Phase 4: User Story 2 — the consent boundary and one honest dispatch with lookup-before-resend
6. **STOP and VALIDATE**: quickstart.md §1 steps 2–3 and §3 (three replays, one task); the MVP is a Task handed off once to a real A2A agent

### Incremental Delivery

1. Setup + Foundational → foundation ready
2. Add User Story 1 → test independently → demo connections (MVP part 1)
3. Add User Story 2 → test independently → demo one hand-off with one task at the agent (MVP complete)
4. Add User Story 3 → test independently → demo monitoring, reply with succession, cancel, push acceleration
5. Add User Story 4 → test independently → demo disconnect, retention clocks and purge
6. Polish: remove the bespoke wire, prove conformance against both unmodified runtimes, ship the Playwright stories, docs, and the writer gates
7. Each story adds value without breaking the previous ones; rollout stays OFF throughout

### Parallel Team Strategy

With multiple developers, after Setup + Foundational:

- Developer A: the backend lane in story order (US1 → US2 → US3 → US4 → removal)
- Developer B: the web lane, one story behind the backend schema task of that story
- Developer C: the iOS lane, one story behind the backend schema task of that story
- Any developer: vendoring and provenance (T004–T007) at any time; the reference-runtime tests and the Playwright stories only after T098

### What is never a task here

- The attended Hermes live run (`quickstart.md` §8; `docs/external-agent-relay-release.md` "Hermes live evidence") spends provider money and requires explicit human approval like `verify-live`; it is a human release step, never run by the implementer, a subagent or a schedule
- Turning `external_agent_relay` ON — gated on the recorded live evidence and on the refreshed iOS build being the only distributed build
- Landing (`scripts/submit_to_trunk.sh`, `git push`), the ASK review on the exact SHA, deploy and the TestFlight/App Store path — post-freeze obligations under ADR-0008

---

## Notes

- `[P]` tasks = different files, no dependencies; granted conservatively (see Path Conventions) so that two lanes never edit one file at once
- `[Story]` label maps a task to its user story for traceability; Setup, Foundational and Polish carry none
- Each user story is independently completable and testable with seeded rows; only US2 → US3 is a hard chain
- Verify tests fail before implementing; commit after each task or logical group; stop at any checkpoint to validate the story
- Line numbers quoted for removals are pre-014 positions from `research.md` Decision F; re-locate them by symbol, not by number, once earlier passes have shifted the file
- Existing files the plan's tree omits but which the work necessarily touches, named here rather than hidden: `mobile/src/app/agents/index.tsx` (mounts the deleted secret sheets), `mobile/src/features/agents/AgentRunSection.tsx` (renders the run card `TaskAgentSection` composes), `backend/tests/test_agent_secrets.py` (owns the inbound-signing purpose cases), the frontend `TaskDetailPanel`/`TaskListPage` suites and the mobile hook suites listed in T084
- Avoid: vague tasks, same-file conflicts, cross-story dependencies that break independence, and any resend from a background thread
- Generated tasks.md is portable planning input only. Do not use it to bypass
  isolated worktrees, TDD, independent review, CI, landing, or release gates.
